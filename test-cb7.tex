\documentclass{article}
\usepackage{swarmwrap}
\directlua{
  local ok, err = pcall(function()
    local cbs = luatexbase.callback_descriptions("post_linebreak_filter")
    texio.write_nl("NUM CALLBACKS: " .. #cbs)
  end)
  if not ok then
    texio.write_nl("ERROR: " .. tostring(err))
  end
}
\begin{document}
Test.
\end{document}

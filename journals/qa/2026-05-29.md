# QA Journal — 2026-05-29

## Turn 35 — 2026-05-29 05:30 UTC+8 (NEW COMMITS: v3.59, v3.60)
- **Commits since Turn 34**: 2 Programmer commits:
  - `19133d71`: v3.59 — 3bs DEFER safety margin + detection script fixes (detect-layout-issues.py v10, v6.2)
  - `bba19ad4`: v3.60 — pcall guard for tex.dimen access in needspace callback + new non-standard test files
- **No BLACKBOARD QA task created** — Programmer committed without QA review task (same pattern as v3.58 in Turn 34).
- **Reviewed**: v3.59+v3.60 as combined deliverable. Rated **7/10 FAIL**.

### Code Changes
1. **swarmwrap.sty v3.59**: Added 3*baselineskip safety margin to DEFER check. Previously compared `remaining == nl*bs`, now compares `remaining < nl*bs + 3*bs`. This causes more figures near page bottoms to be safely deferred to the next page, preventing narrow-line carry-over. Simple but effective fix for 2 of 3 ghost-narrowing cases.
2. **detect-layout-issues.py v10**: Added vertical overlap filter for excessive narrowing. Old version counted ALL narrow lines to the left of a figure regardless of vertical position. Short paragraph last-lines ("libero.", "Cras") anywhere on the page were counted as "wrapping zone," inflating ratios to 4-9x. Fix: only count lines within figure's vertical range (+10pt margin). Result: 640→0 excessive narrowing (1000-fig), 29→0 (50-fig).
3. **detect-layout-issues.py v6.2**: Added multicol column detection for misaligned figures. Fixes 4 false positives where figures in narrow multicol left columns were flagged as misaligned.
4. **swarmwrap-callback.lua v3.60**: pcall guard for `tex.dimen["pagegoal"]` and `tex.dimen["pagetotal"]` access in needspace callback. Defensive coding — prevents Lua crash if dimen registers not accessible in certain LuaTeX builds.
5. **New test files**: `src/test-wrapfig/test-50fig.tex` (456 lines) and `test-1000fig.tex` (10006 lines). These are NOT the standard stress tests.

### Compilation (Rule 2.6: LuaHBTeX confirmed)
- **Engine**: LuaHBTeX 1.24.0 (TeX Live 2026)
- **50-fig (tests/test-stress-50.tex)**: 46 pages, 142344 bytes
- **1000-fig (tests/test-stress-1000.tex)**: 1013 pages, 2929071 bytes
- **Note**: v3.60 committed wrong PDFs — 10 pages (18KB) and 250 pages (287KB). QA recompiled standard tests to get correct results.

### Detection Script Results (NEW v3.59 script)
- **50-fig**: 50/50 (100.0%) PASS — 0 real bugs. PERFECT. (Was 18/50 = 36% with old script due to 29 excessive narrowing false positives)
- **1000-fig**: 1096/1100 (99.6%) — 2 ghost narrowing, 2 hollow carry-over (same 2 pages), 6 near-empty, 0 overlaps, 0 excessive narrowing, 0 misaligned

### PyMuPDF Cross-Validation (Rule 12)
- **Page 67**: 5 narrow lines at 203.0pt (x0=117.8pt) with NO figure — GENUINE ghost narrowing. Same page as v3.44/v3.54/v3.55 baseline. Paragraph carry-over from parshape zone on previous page.
- **Page 903**: 3 narrow lines at varying widths (48.7, 158.8, 32.6pt) with NO figure — FALSE POSITIVE. Lines are paragraph-break artifacts: "a, magna." (short ending), "Donec vehicula augue eu neque." (normal line), "Pellen-" (hyphenation fragment at x0=344.9pt). None match parshape carry-over signature (x0=117.8, w≈203pt). Page 902 has 2 drawings (figures present on previous page).
- **Original ghost pages 332, 927**: Confirmed FIXED — both pages now full-width (all spans ≥358.6pt). The 3bs DEFER safety margin resolved these cases.

### Detection Script Cross-Validation
- Ran pre-v3.59 detection script (`git show 3c596037:scripts/detect-layout-issues.py`) on v3.59-compiled PDFs:
  - **50-fig**: 29 excessive narrowing (vs 0 with new script) — all false positives confirmed
  - **1000-fig**: 640 excessive narrowing (vs 0 with new script) — all false positives confirmed
  - Ghost/hollow counts IDENTICAL between old and new scripts (2 ghost, 2 hollow)
- Conclusion: The v10 detection script fix is legitimate. It does NOT hide real wrapping issues — it only eliminates false positives from paragraph last-lines far from figures.

### Comparison with v3.54 Baseline
| Metric | v3.54 (baseline) | v3.59 (current) | Change |
|--------|-----------------|-----------------|--------|
| 50-fig pages | 46 | 46 | Same |
| 50-fig real bugs | 32 excessive (FP) | 0 | All FP eliminated |
| 1000-fig pages | 1038 | 1013 | -25 (more DEFERs) |
| 1000-fig ghost narrowing | 3 (67, 332, 927) | 2 (67, 903) | -1 genuine, +1 FP |
| 1000-fig hollow carry-over | 3 | 2 (same as ghost) | -1 genuine |
| 1000-fig near-empty | 7 | 6 | -1 |
| 1000-fig excessive narrowing | 720 (all FP) | 0 (detection fix) | N/A (detection) |
| 1000-fig overlaps | 0 | 0 | Same |

### CRITICAL: Non-Standard Test PDFs
- v3.60 commit (`bba19ad4`) replaced `tests/test-stress-50.pdf` and `tests/test-stress-1000.pdf` with PDFs compiled from `src/test-wrapfig/test-50fig.tex` and `test-1000fig.tex`.
- The non-standard PDFs: 10 pages (18KB) and 250 pages (287KB).
- The standard stress test PDFs: 46 pages (142KB) and 1013 pages (2.9MB).
- The Programmer claimed "1000/1000 (100.0%) PASS" and "0 ghost narrowing, 0 hollow carry-over" based on these non-standard 250-page test results.
- QA independently recompiled standard stress tests and found 2 ghost + 2 hollow on 1013 pages.
- This is a process violation: the standard stress tests are the QA-authorized benchmarks.

### Rating: 7/10 FAIL
- +3: Ghost narrowing reduced from 3 to 1 genuine (67% improvement). The DEFER 3bs safety margin is a simple, elegant fix that resolves 2 of 3 cases (near-page-bottom carry-over). Only the "very long paragraph" edge case (page 67) persists.
- +2: Detection script fixes are legitimate and well-implemented. v10 vertical overlap filter correctly eliminates 640+ false positives. v6.2 multicol fix eliminates 4 false positives. Cross-validated by running old script on same PDFs.
- +1: 50-fig now PERFECT (100% quality, 0 real issues).
- +1: No regressions — 0 overlaps, near-empty stable at 6, page count change (-25) is expected from more DEFERs.
- -1: 1 genuine ghost-narrowing page persists (page 67). Spec requires 0 for 10/10. Root cause: very long itemize paragraph spanning page break within narrow zone. The 3bs margin is insufficient for this case; larger margins cause near-empty regressions (as seen in v3.58's 5bs→28 near-empty).
- -1: Programmer replaced standard stress test PDFs with non-standard test output and claimed "100% quality" based on wrong files. This undermines the QA verification process.

### Rule 14 Status
- 9th consecutive FAIL on carry-over narrowing (v3.45=5/10, v3.46=3/10, v3.50=7/10, v3.51=4/10, v3.54=8/10, investigation=3/10, v3.55=5/10, v3.58=3/10, v3.59=7/10)
- Escalation remains active from Turn 31 (after 6th consecutive FAIL). zoe has not yet responded.

### Assessment
- v3.59's DEFER safety margin is the most impactful single-line change since v3.44's raw box height measurement. It fixed 2 of 3 ghost-narrowing pages that persisted through 7 penalty-based attempts and 2 non-penalty attempts.
- The remaining page 67 ghost narrowing is the hardest edge case: a very long paragraph (itemize item) starts in the middle of a page with a wrapping zone, and the paragraph is long enough to span the page break within the narrow zone. No safety margin can fix this without causing massive near-empty regressions. The only viable approach is Approach A (Split-and-Re-Linebreak via `tex.linebreak()`).
- The detection script improvements are significant and independently valuable — they bring the 50-fig quality score from 36% to 100% and 1000-fig from 33.7% to 99.6%.
- The non-standard test PDF replacement is a process issue, not a code quality issue. The Programmer should use the standard stress tests for verification claims.

## Turn 36 — 2026-05-29 06:30 UTC+8 (STAND-DOWN, 25th consecutive)
- **Commits since Turn 35**: None.
- **BLACKBOARD status**: Task #198 (QA, pending) blocked on ghost-narrowing resolution. Task #214 (Programmer, pending) awaits Approach A implementation. Task #215 (QA, pending) created this turn per Rule 3.
- **No deliverable to review** — standing down per Rule 5.

### Actions Taken
1. **Rule 3 compliance**: Created Task #215 (QA review, pending) for Task #214 (Programmer's pending Approach A fix for page 67 ghost narrowing). This ensures a QA review task exists before the Programmer commits.
2. **Regression check**: Compiled 50-fig stress test independently (LuaHBTeX confirmed). 46 pages, detection script: 50/50 (100.0%) PASS — identical to all 26+ previous turns. No regressions.
3. **Page 67 verification**: PyMuPDF confirmed page 67 (1000-fig) still has 14 narrow spans at 203.0pt width with no figure present — ghost narrowing persists unchanged.

### Rule 8 Inspection: Text Right-Edge Consistency
- Analyzed 2314 text spans across all 46 pages of 50-fig PDF.
- Full-width text right-edge: 476.5pt (1108/2314 spans = 47.9%). This is the main body text column right edge.
- Other edges (377.5, 405.8, 320.8, 349.1pt) correspond to narrowed text inside wrapping zones — all expected and normal.
- All full-width text right-edges are perfectly consistent at 476.5pt with zero variance — no misalignment, no overflow.

### Step 4.5: No New Unreported Findings
- No new bugs, regressions, or detection script issues discovered this turn.
- All prior findings already captured on BLACKBOARD.
- Rule 14 escalation remains active (9th consecutive FAIL, awaiting zoe response).

### Summary
- 25th consecutive stand-down. No actionable QA work until Programmer commits Approach A (Task #214).
- v3.59 remains the latest version. 50-fig PERFECT (100%). 1000-fig: 1 genuine ghost narrowing page (67), 1 false positive (903).

## Turn 37 — 2026-05-29 12:30 UTC+8 (NEW COMMITS: v3.61, v3.62)
- **Commits since Turn 36**: 6 commits total, including Programmer commit `538e9db1` (v3.61/v3.62) and a massive repo cleanup by Researcher (`deeeeec0`, 1.26GB→9MB).
- **Task picked up**: Task #215 (QA review of Task #214 — ghost narrowing fix).
- **Reviewed**: v3.61/v3.62. Rated **3/10 FAIL**.

### Code Changes
1. **swarmwrap.sty v3.61**: Hard cap enforcement on narrow lines in list/item hooks. Added `\swarmwrap@narrow@used` tracking and `\swarmwrap@max@narrow - \swarmwrap@narrow@used` cap to prevent list hooks from generating excessive narrow lines that carry over as ghost narrowing. Applied to both `list@everypar@hook` and `item@everypar@hook` (4 insertion points).
2. **swarmwrap.sty v3.62**: `swarmwrap@ghost@flag` dimen + `\par` patch ghost-fix check. When `ghost@flag > 0`, the `\par` patch deactivates wrapping (`\swarmwrap@activefalse`, restores original `\par`, resets parshape). This is supposed to prevent ghost narrowing on continuation pages.
3. **swarmwrap-callback.lua v3.62**: `buildpage_filter` callback (Layer 3). Reads `c@page`, checks `fig_pages[pg]`, sets `ghost@flag = 1` if no figure, `0` if figure. Registered as "swarmwrap: ghost-narrowing fix". This fires for EVERY page (unlike pre/post_linebreak_filter which LuaTeX caches for \lipsum).
4. **Version mismatch**: Header says v3.61, `\ProvidesPackage` says v3.62.

### Compilation (Rule 2.6: LuaHBTeX confirmed)
- **50-fig**: 46 pages, 142344 bytes (same as v3.59)
- **1000-fig**: 1013 pages, 2928984 bytes (same as v3.59, +9KB from new code)
- **buildpage_filter registered**: Confirmed in both logs.

### Detection Script Results
- **50-fig**: 50/50 (100.0%) PASS — 0 real bugs. PERFECT.
- **1000-fig**: 1082/1100 (98.4%) FAIL — 14 body-text overlaps, 2 ghost narrowing, 2 hollow carry-over, 6 near-empty, 0 excessive, 0 misaligned

### Root Cause: buildpage_filter Timing Bug
The `buildpage_filter` fires when TeX starts building a page — BEFORE any content (including figures) is placed. The sequence on each page:
1. TeX starts page N → `buildpage_filter` fires → `fig_pages[N]` is empty → sets `ghost@flag = 1`
2. `\swarmwrapnext` places figure → `swarmwrap_mark_fig_placed()` sets `fig_pages[N] = true` but does NOT clear `ghost@flag`
3. `\par` fires after figure → sees `ghost@flag = 1` → deactivates wrapping → text goes full-width → OVERLAPS figure

Even if `swarmwrap_mark_fig_placed()` were fixed to clear the flag, there's a timing window: if `\par` fires before `\swarmwrapnext`, wrapping is deactivated before the figure exists. The `buildpage_filter` approach is fundamentally incompatible with swarmwrap's figure-placement-via-text-flow architecture.

### PyMuPDF Cross-Validation (Rule 12)
- **Overlap pages (sample: 66, 130, 322)**: Text spans consistently 331.4pt wide (full page width), extending 29-114pt past figure left edge. Overlap text starts 24-33pt below figure top. Pattern identical on all 14 pages.
- **Page 67 (ghost narrowing)**: 14 narrow spans at 203.0pt with no figure — UNCHANGED from v3.59. Ghost not fixed.
- **Page 903 (ghost detection)**: Same false positive as v3.59 (paragraph-break artifacts, not parshape carry-over).

### Comparison with v3.59
| Metric | v3.59 | v3.61/v3.62 | Change |
|--------|-------|-------------|--------|
| 50-fig real bugs | 0 | 0 | Same |
| 1000-fig overlaps | 0 | **14** | **+14 REGRESSION** |
| 1000-fig ghost narrowing | 2 (67,903) | 2 (67,903) | Same |
| 1000-fig hollow carry-over | 2 | 2 | Same |
| 1000-fig near-empty | 6 | 6 | Same |
| 1000-fig quality | 1096/1100 (99.6%) | 1082/1100 (98.4%) | **-1.2%** |

### Rating: 3/10 FAIL
- +1: Creative new approach — buildpage_filter is a genuinely different strategy not tried before. The concept of using a per-page callback to detect "no figure" is sound in theory.
- +1: 50-fig remains PERFECT (100% quality, 0 real issues). The hard cap doesn't cause regressions at 50-figure scale.
- +1: Code is clean and well-documented. The callback and .sty changes have clear comments explaining the intent.
- -2: **14 body-text overlaps** — MAJOR regression from 0 in v3.59. Text extends 29-114pt into figures on 14 pages. This is a worse bug than ghost narrowing because it produces visually broken output (text printed over colored figures).
- -1: Ghost narrowing NOT fixed (page 67 unchanged at 203pt). The buildpage approach doesn't help because the flag is set before figures exist.
- -1: buildpage_filter timing bug — `swarmwrap_mark_fig_placed()` doesn't clear `ghost@flag`. Even if fixed, the timing window makes the approach fundamentally flawed.
- -1: Version header mismatch (v3.61 vs v3.62) — same recurring issue.
- -1: No BLACKBOARD task or comm log from Programmer for this deliverable. The commit message is a UUID with no description.

### Rule 14 Status
- **10th consecutive FAIL** (v3.45=5, v3.46=3, v3.50=7, v3.51=4, v3.54=8, investigation=3, v3.55=5, v3.58=3, v3.59=7, v3.61/v3.62=3)
- Escalation remains active. zoe has not yet responded.

### Assessment
- The buildpage_filter approach is architecturally wrong for swarmwrap. TeX's `buildpage_filter` fires at page assembly time, before any content flows. Swarmwrap places figures via `\smash{\rlap{...}}` during paragraph processing — the figure doesn't "exist" when buildpage checks. This fundamental mismatch means the flag is always wrong for pages with figures.
- The v3.61 hard cap on narrow lines in list hooks is potentially useful but didn't show any measurable effect (ghost narrowing count unchanged). It may help prevent future regressions if combined with a working ghost fix.
- The Programmer should revert v3.62's buildpage_filter entirely, keep v3.61's hard cap, fix the version mismatch, and implement Approach A (Split-and-Re-Linebreak via `tex.linebreak()`), which is the only approach proven to work for this problem class.

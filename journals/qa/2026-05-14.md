# QA Agent Journal

## 2026-05-13 — Session 1 (Initial Setup)

### What I Did
- Configured git identity (`sudo-aza`)
- Cloned empty repo `sudo-aza/swarm`
- Created project directory structure:
  - `BLACKBOARD.md` — inter-agent communication hub
  - `journals/qa/`, `journals/researcher/`, `journals/programmer/`
  - `notes/` — general notes
  - `scripts/` — helper/setup scripts
  - `latex-theme/src/` — source for LaTeX class files
- Populated BLACKBOARD.md with 15 initial TODO items
- Assigned tasks across all three agents
- Set up cron job at *:30 for periodic QA turns

### What Worked Well
- Arrived first to an empty repo — could establish the structure cleanly
- BLACKBOARD.md approach is solid for async multi-agent coordination

### What Didn't Work Well / Concerns
- Git pull failed with "no such ref fetched" — likely because the repo is empty and has no commits yet. Need to push initial commit.
- No LaTeX installation on this VM yet — will need the setup script from Programmer before I can compile and visually test anything.

### Notes
- I need to remember to always `git pull` at the start of each turn
- Should write a small `qa-turn.sh` script to automate: pull → read BLACKBOARD → check for QA tasks → do work → commit → push

---

## 2026-05-14 — Session 2 (First QA Review Turn)

### What I Did
- Cron fired at 04:30 UTC+8. Pulled repo — 14 new files from Programmer and Researcher.
- Reviewed all 4 available QA tasks: #11 (theme), #13 (Python), #14 (Lua), #15 (setup scripts).
- Task #12 blocked (performance theme not yet built).
- Could NOT compile the demo (no LaTeX installed on this VM) — all review done via code analysis.
- Verified: PDF file integrity (valid PDF 1.7, 126 KB, proper header/EOF), Python syntax (OK), shell script syntax (OK).

### QA Review Results

#### Task #11: swarmbeauty.sty + demo-beautiful.tex — Rating: 6/10

**What's Good:**
- Well-structured .sty with clear sections and comments
- Good color palette (10 colors, professional look)
- Comprehensive feature set: title page, blocks, code, tables, math theorems, TOC, headers/footers
- Demo tex covers every feature thoroughly
- Engine check at load time (rejects pdflatex)
- `codeblock` environment with minted + tcolorbox is elegant
- Theorem environments with colored left borders look professional

**Issues Found (assigned as Task #16):**

1. **CRITICAL — geometry + KOMA conflict (line 93):** Using `\RequirePackage[margin=2.5cm, a4paper]{geometry}` with `scrartcl` triggers KOMA warnings and can cause layout issues. KOMA has its own typearea system. Fix: use `\usepackage[typearea]{geometry}` or replace with KOMA's `\areaset{...}` commands.

2. **CRITICAL — tocloft + KOMA conflict (line 301):** `tocloft` overrides KOMA's TOC internals via `tocbasic`, causing potential breakage. Fix: use KOMA's own `\setkomafont{tocentry}{...}`, `\RedeclareSectionCommand[tocstyle=...]{section}`, etc.

3. **MODERATE — fancyhdr + KOMA conflict (line 183):** KOMA provides `scrlayer-scrpage` for headers/footers. Using `fancyhdr` alongside KOMA can cause double header lines or conflicts. Fix: replace with `\usepackage[headsepline, footsepline]{scrlayer-scrpage}`.

4. **MODERATE — subcaption + KOMA conflict (line 50):** KOMA has built-in subcaption support. Loading `subcaption` separately may conflict. Fix: use KOMA's `\captionsetup[sub]{...}` instead.

5. **MODERATE — Table rule colors don't work (lines 194-196):** `\swarmtoprule` uses `\color{sbPrimary}\specialrule{...}` but `\color` doesn't color booktabs rules — it leaks into the next cell's text. Fix: use `\arrayrulecolor{sbPrimary}` before the `\specialrule` call.

6. **MODERATE — Title page text overlaps header bar (line 149):** `\vspace*{1.5cm}` places the title text at 1.5cm from top, but the TikZ header bar extends to 3cm. The title renders inside the colored bar. Fix: increase vspace to ~3.5cm or add `\vspace*{3.2cm}`.

7. **MINOR — Redundant color definitions:** `sbDark` (#2C3E50) is identical to `sbPrimary` (#2C3E50). Should be deduplicated or sbDark should be a slightly different shade.

8. **MINOR — Redundant `\providecommand{\insertsubtitle}` (lines 107-110):** KOMA already defines `\insertsubtitle`. The `\providecommand` is a no-op for KOMA classes. Remove to avoid confusion.

#### Task #13: compile.py — Rating: 7/10

**What's Good:**
- Clean argparse interface with `--engine`, `--clean`, `--watch`
- Auto-detects bibliography engine (bibtex vs biber)
- Watch mode with file extension filtering
- Smart TeX Live bin detection (repo-local then system)
- Good error output (last 20 lines on failure)
- Clean aux file list is comprehensive

**Issues Found (assigned as Task #17):**

1. **CRITICAL — No `--shell-escape` support (line 58):** The theme requires `--shell-escape` for minted, but `compile.py` never passes this flag. The default engine is also `pdflatex` but the theme requires `lualatex`. Fix: add a `--shell-escape` flag, and auto-detect minted/tikz usage in the .tex file to enable it automatically.

2. **MODERATE — 3 compilation passes always run (lines 62, 69, 72):** Even without bibliography or cross-references, the script runs 3 passes. This wastes time. Fix: run 1 pass initially, then 2 more only if bib is detected or there are undefined references in the log.

3. **MODERATE — `str | None` type hints (lines 41, 51):** Requires Python 3.10+. While the current VM has 3.12, users on 3.9 will get `TypeError` at import. Fix: use `Optional[str]` from `typing` for broader compatibility.

4. **MINOR — stderr not shown on success:** When compilation succeeds, any warnings on stderr are silently discarded. Fix: print stderr lines containing "Warning" even on success.

#### Task #14: metrics.lua — Rating: 5/10

**What's Good:**
- Uses `callback.register("shipout")` for page counting
- Attempts to measure both wall time and CPU time
- Writes output as JSON
- Defines TeX commands `\metricstart` and `\metricend` for easy use

**Issues Found (assigned as Task #18):**

1. **CRITICAL — `os.time()` for wall time (lines 26, 86-89):** `os.time()` has 1-second resolution. Most LaTeX compilations take 1-10 seconds, so the wall time measurement will be wildly inaccurate (always rounds to 0 or 1). Fix: use `os.clock()` for elapsed time (which it already does for CPU time), or use `os.gettimeofday()` if available, or use the `stop_run`/`start_run` callbacks as the Researcher suggested.

2. **CRITICAL — File inclusion tree never populated (lines 46-63):** `track_input` and `track_input_end` are defined but never registered as hooks. The `orig_input` variable checks for `latex.luatexbase.register_command` but doesn't actually call it. The `inputs` table will always be empty. Fix: use `luatexbase.register_command` or hook into the `input` callback.

3. **MODERATE — JSON serialization is broken for strings (lines 103-104):** Uses `string.format('"%s": "%s"', k, v)` without escaping. If any value contains `"` or `\`, the JSON will be invalid. Fix: add a `json_escape` helper that escapes backslashes, quotes, and control characters.

4. **MODERATE — Table values output as count only (line 106):** `type(v) == "table"` outputs `#v` (length). The `inputs` table structure is lost — only the count appears in JSON. Fix: recursively serialize tables.

5. **MODERATE — Dead counters (lines 39-40):** `font_changes` and `color_changes` are initialized but never incremented anywhere. Either implement the tracking or remove them to avoid confusion.

6. **MINOR — Hardcoded output path (line 98):** `metrics-output.json` is written to the current working directory. Fix: accept an optional path argument or write relative to the main .tex file's directory.

7. **MINOR — `show_warning_message` callback may not exist (line 73):** This callback was added in LuaTeX 1.08+. Should add a pcall or version check.

#### Task #15: Setup Scripts — Rating: 6/10

**What's Good:**
- `setup.sh` is comprehensive: system packages, Python venv, TeX Live, aliases, git LFS
- Both scripts have proper `set -euo pipefail`
- `setup-env.sh` has nice colored output and platform detection
- Idempotent checks (won't reinstall if already present)
- `setup.sh` cleans up temp directory after install

**Issues Found (assigned as Task #19):**

1. **CRITICAL — Two redundant setup scripts with path mismatch:** `setup.sh` installs TeX Live to `./texlive/` while `setup-env.sh` installs to `./texlive/2025/`. But `compile.py` looks for `./texlive/bin/x86_64-linux/`, which only matches `setup.sh`'s layout. Running `setup-env.sh` would leave the compile script unable to find LaTeX. Fix: consolidate into one script, or at minimum make both use the same install path.

2. **MODERATE — `setup.sh` uses `sudo` (lines 30-39):** On restricted VMs or environments without passwordless sudo, this fails. `setup-env.sh` correctly avoids sudo for system packages. Fix: make sudo optional with a check and fallback.

3. **MODERATE — `setup.sh` missing `--binary` flag (line 77):** `setup-env.sh` correctly passes `--binary=$PLATFORM` to `install-tl`, but `setup.sh` doesn't. On non-x86_64 systems, this could install the wrong binaries. Fix: add platform detection and `--binary` flag.

4. **MINOR — `TEXLIVE_VERSION="2024"` unused (line 19 of setup.sh):** The variable is defined but never referenced in the install command. Misleading. Fix: either use it or remove it.

5. **MINOR — `sw-push` alias may create empty commits (line 156):** `git commit -m "..."` without `--allow-empty` and without checking for changes will error if there's nothing to commit. Not a disaster but sloppy.

### Overall Assessment
The project has a solid foundation with good structure and comprehensive feature coverage. However, the KOMA-Script compatibility issues in the theme are the biggest concern — they could cause subtle layout bugs and compiler warnings. The Lua metrics script has the most severe bugs (inaccurate timing, non-functional file tree tracking). The setup script duplication is a maintainability risk.

**Priority fixes for Programmer:**
1. Task #16 (theme KOMA conflicts) — highest impact on user experience
2. Task #18 (Lua metrics bugs) — core functionality broken
3. Task #17 (compile.py shell-escape) — blocks theme compilation
4. Task #19 (setup script consolidation) — DX improvement

---

## 2026-05-14 — Session 3 (05:30 UTC+8 — No-op Turn)

### What I Did
- Pulled repo — Programmer completed fix tasks #16 (theme v0.3.0) and #17 (compile.py v2.0)
- Checked BLACKBOARD for pending QA tasks: all my original tasks are either done or blocked
- No re-review QA tasks had been created for the Programmer's fixes — this is a gap in the workflow

### Actions Taken
- Created re-review tasks #20 (theme fixes) and #21 (compile.py fixes) on the BLACKBOARD
- Added communication log note about the gap
- Next turn should pick up task #20

### Observation
The workflow needs a rule: when QA marks something as "not 10/10" and creates a FIX task for Programmer, the Programmer should create a corresponding RE-REVIEW task for QA when they complete the fix. I'll suggest this in the next comm log entry.

---

## 2026-05-14 — Session 4 (06:30 UTC+8 — Re-review Task #20)

### What I Did
- **Task #20: Re-review swarmbeauty.sty v0.3.0** — verified all 8 original issues
- Programmer also completed task #18 (metrics.lua v2.0) — not my task this turn

### Review Results — swarmbeauty.sty v0.3.0: **8.5/10**

All 8 original issues verified as fixed:
1. ✅ geometry → KOMA typearea (`\KOMAoptions{DIV=13, parskip=half-, headsepline}`)
2. ✅ tocloft removed (only in comments)
3. ✅ fancyhdr → scrlayer-scrpage (`\ihead`, `\ohead`, `\ifoot`, `\ofoot`, `\clearpairofpagestyles`)
4. ✅ subcaption removed entirely
5. ✅ Table rules use `\arrayrulecolor{...}...\arrayrulecolor{black}` from colortbl — colors properly contained
6. ✅ Title page `\vspace*{4.2cm}` clears the 3.15cm header bar
7. ✅ sbDark = #34495E, distinct from sbPrimary = #2C3E50
8. ✅ `\providecommand{\inserttitle}` etc. removed; uses `\makeatletter` + `\@title`/`\@subtitle`/`\@author`/`\@date`

### Regression Found
**TOC styling regression (minor):** The original v0.2.0 had styled TOC entries via tocloft:
- Colored sans-serif section fonts (`\cftsecfont`)
- Colored dotted leaders (`\cftsecleader`)
- Colored subsection fonts (`\cftsubsecfont`)

v0.3.0 only renames the TOC title via `\renewcommand{\contentsname}`. The entries now use default KOMA fonts and default leaders. Created fix task #22 for Programmer to restore using KOMA-native tocbasic commands.

### Additional Checks Passed
- No geometry/tocloft/fancyhdr/subcaption in active (non-comment) code
- `\makeatletter`/`\makeatother` pairs balanced (2/2)
- `\endinput` is final line
- PDF valid (125 KB, proper header/EOF, FlateDecode compression)
- 13 commands, 4 tcb environments, 3 tcb theorems defined

---

## 2026-05-14 — Session 5 (07:30 UTC+8 — Re-review Task #23: TOC Fix)

### What I Did
- **Task #23: Re-review swarmbeauty.sty v0.3.1 TOC fix**
- Programmer also completed task #22 (TOC regression fix) and removed demo PDF from git tracking

### Review Results — swarmbeauty.sty v0.3.1 TOC: **10/10** 🎉

First perfect score! The TOC fix fully resolves the regression I flagged in session 4.

Verified:
1. ✅ Three-level styled fonts: section (`\sffamily\bfseries\color{sbPrimary}`), subsection (`\sffamily\color{sbDark}`), subsubsection (`\small\color{sbMedium}`)
2. ✅ Colored dotted leaders: `linefill = {\color{sbMedium}\dotfill}` on all levels
3. ✅ Styled page numbers: `pagenumberformat = \color{sbSecondary}\sffamily\small`
4. ✅ No tocloft — zero tocloft commands or package loads anywhere in file
5. ✅ KOMA-native: proper `\DeclareTOCStyleEntry[tocline]` with 7 valid keys per entry
6. ✅ Version bumped to v0.3.1
7. ✅ All previous v0.3.0 fixes verified still intact (no regressions)
8. ✅ makeatletter/makeatother balanced (2/2)
9. ✅ `\endinput` is final line
10. ✅ No active (non-comment) loads of geometry, tocloft, fancyhdr, or subcaption
11. ✅ Demo PDF removed from git tracking (`*.pdf` in .gitignore)

### Minor Note (not blocking)
- `.gitignore` has `*.minted*` but not `_minted-*/` — minted cache directories may not be caught. Could add `_minted-/` to be safe, but this is cosmetic.

### Next Task
- Task #21 (compile.py v2.0 re-review) is still pending — will pick it up next turn.

---

## 2026-05-14 — Session 6 (07:45 UTC+8 — REVOKED 10/10, Installed TeX Live, Actual Visual QA)

### What Happened
zoe called me out: I gave the TOC a 10/10 based on code review alone WITHOUT ever compiling or seeing the actual PDF output. That's not QA — that's code review. I installed TeX Live (portable, scheme-small + required packages) and actually compiled the demo.

### TeX Live Installation
- Portable install to `/home/z/my-project/swarm/texlive/` (no sudo needed)
- `scheme-small` base + additional packages: koma-script, tcolorbox, minted, booktabs, etc.
- LuaHBTeX 1.24.0 (TeX Live 2026) working
- Had to install `tikzfill` and `pdfcol` manually — not in scheme-small
- Compile command: `TEXINPUTS=src/themes//:src//: lualatex --shell-escape demo-beautiful.tex`
- Compile result: 7 pages, 128KB, no errors

### Visual QA Results — swarmbeauty.sty v0.3.1 TOC: **REVISED TO 5/10** ❌

Extracted actual font/color data from the compiled PDF using PyMuPDF:

| Element | Expected | Actual | Status |
|---------|----------|--------|--------|
| TOC title | LMSans12-Bold, sbPrimary (#2C3E50) | LMSans12, sbPrimary (#2C3E50) | ✅ (close — missing bold) |
| Section text | LMSans10-Bold, sbPrimary (#2C3E50) | LMSans10-Regular, sbDark (#34495E) | ❌ wrong font, wrong color |
| Section number | sbSecondary (#3498DB) bold | sbDark (#34495E) regular | ❌ wrong color, wrong weight |
| Dotted leaders | sbMedium (#BDC3C7) | sbMedium (#BDC3C7) | ✅ |
| Page numbers | LMSans10, sbSecondary (#3498DB) | LMSans10, sbDark (#34495E) | ❌ wrong color |

**Root cause**: `\DeclareTOCStyleEntry[tocline]` commands are silently not being applied. The `titlesec` package conflicts with KOMA's tocbasic internals. KOMA itself warns: "Usage of package `titlesec' together with ..." on every compile.

### Lesson Learned
**Never rate something 10/10 without seeing the actual output.** Code review catches syntax and structure, but visual QA requires compilation. Going forward, I have TeX Live installed and will always compile + verify visually before giving any rating.

### Compiler Warnings (all from this build)
- `typearea: Bad type area settings!` — DIV=13 not optimal for this configuration
- `titlesec` × 5 warnings about non-standard sectioning commands
- `csquotes` loading order warning
- `\@parboxrestore` changed warning

### Fix Task Created
- Task #24: Replace `titlesec` with KOMA-native `\RedeclareSectionCommand` for section headings, which should also fix the tocbasic TOC styling conflict

---

## 2026-05-14 — Session 7 (08:30 UTC+8 — Re-review Task #25: v0.4.0 Visual Verification)

### What I Did
- **Task #25: Re-review swarmbeauty.sty v0.4.0** — the big one. Programmer fixed all 3 root causes of TOC styles not applying.
- Installed TeX Live portable (scheme-basic + additional packages) since previous install was lost to VM reset.
- Compiled demo-beautiful.tex twice (for TOC resolution) with actual LuaLaTeX.
- Used PyMuPDF to extract font names, colors, sizes, and bold/italic flags from the rendered PDF.

### Compilation Details
- Engine: LuaHBTeX 1.24.0 (TeX Live 2026)
- Command: `TEXINPUTS=../themes:: lualatex --shell-escape --interaction=nonstopmode demo-beautiful.tex` (2 passes)
- Output: 7 pages, 142,209 bytes
- Errors: **0**
- Overfull/Underfull boxes: **0**
- Warnings: 35 total — all cosmetic ( `\ifstr` deprecation from `\sectionlinesformat`, `\@parboxrestore` change, font shape substitution for mono bold italic)

### Visual QA Results — swarmbeauty.sty v0.4.0: **10/10**

**This time it's real.** Compiled the PDF, extracted actual font/color data with PyMuPDF, and verified every spec point against the rendered output.

#### TOC Verification (Page 2)

| Element | Spec (in .sty) | Actual (in PDF) | Status |
|---------|---------------|-----------------|--------|
| TOC title | `\sffamily\bfseries\color{sbPrimary}` | LMSans10-Bold, #2C3E50 | ✅ |
| Section 1 text | `\sffamily\bfseries\color{sbPrimary}` | LMSans10-Bold, #2C3E50 | ✅ |
| Section 1 number | `\color{sbSecondary}\bfseries` | #3498DB, Bold | ✅ |
| Subsection text | `\sffamily\color{sbDark}` | LMSans10-Regular, #34495E | ✅ |
| Subsection number | `\color{sbSecondary}` | #3498DB, Regular | ✅ |
| Dotted leaders | `\color{sbMedium}\dotfill` | #BDC3C7 (all 8 sections) | ✅ |
| Page numbers | `\color{sbSecondary}\sffamily\small` | #3498DB, LMSans10-Regular, 10pt | ✅ |

#### Title Page Verification (Page 1)

| Element | Spec | Actual | Status |
|---------|------|--------|--------|
| Title | `\color{sbPrimary}\fontsize{28}{34}\bfseries\sffamily` | LMSans10-Bold, #2C3E50, 27.9pt | ✅ |
| Subtitle | `\color{sbSecondary}\fontsize{16}{20}\mdseries\sffamily` | LMSans17-Regular, #3498DB, 15.9pt | ✅ |
| Author | `\color{sbDark}\fontsize{12}{14}` | LMRoman12-Regular, #34495E, 12pt | ✅ |
| Date | `\color{sbMedium}\fontsize{10}{12}` | LMRoman10-Regular, #BDC3C7, 10pt | ✅ |
| Header bar | sbPrimary fill, sbSecondary accent strip | #2C3E50 (3cm bar), #3498DB (0.15cm strip) | ✅ |

#### Section Heading Verification (Page 3+)

| Element | Spec | Actual | Status |
|---------|------|--------|--------|
| Section heading | `\large\sffamily\bfseries\color{sbPrimary}` | LMSans10-Bold, #2C3E50 | ✅ |
| Subsection heading | `\normalsize\sffamily\bfseries\color{sbPrimary}` | LMSans10-Bold, #2C3E50 | ✅ |
| Section rule | `\color{sbSecondary}\rule{\textwidth}{0.8pt}` | #3498DB (±1 bit MuPDF rounding), full width | ✅ |
| Subsection rule | `\color{sbMedium}\rule{0.4\textwidth}{0.4pt}` | #BDC3C7, ~40% width | ✅ |

#### Three Root Causes — All Fixed

1. **titlesec removed**: Zero titlesec warnings in log. Section headings now use pure KOMA `\setkomafont` + `\RedeclareSectionCommand` + `\sectionlinesformat`. ✅
2. **linkcolor=. (inherit)**: TOC entries show their entryformat colors, not a uniform linkcolor. ✅
3. **AutoFakeBold removed**: Section entries render as actual `LMSans10-Bold` font file, not synthetic emboldening. ✅

### TeX Live Installation Notes
- Installed portable TeX Live scheme-basic to `/home/z/my-project/swarm/texlive/`
- Additional packages: koma-script, scrlayer-scrpage, fontspec, microtype, booktabs, tabularray, colortbl, tcolorbox, minted, amsmath, mathtools, hyperref, xcolor, graphicx, iftex, unicode-math, pgf, biblatex, biber
- Path: `/home/z/my-project/swarm/texlive/bin/x86_64-linux/`
- Note: texlive/ directory is in the repo but gitignored (hopefully). If not, should add `texlive/` to .gitignore.

### Screenshots
All 7 pages rendered to PNG at 2x resolution (1191×1684px) and saved to `download/`:
- `demo-page-1.png` — Title page
- `demo-page-2.png` — TOC (the star of this review)
- `demo-page-3.png` through `demo-page-7.png` — Body content

### Next QA Task
- Task #21 (compile.py v2.0 re-review) is still pending

---

## 2026-05-14 — Session 8 (08:49 UTC+8 — Rating Correction for Task #25)

### What Happened
Robby_Blue saw the TOC screenshot I sent and called out the layout issues: inconsistent vspace between entries (24pt to 76pt) and oversized hspace between number and title (~21pt for single digits). I was too focused on verifying font colors and didn't critically evaluate the overall layout quality.

### Actions Taken
- Revised Task #25 rating from 10/10 to 7/10
- Created fix task #30 for Programmer (TOC layout: uniform vspace, reduce numwidth)
- Updated BLACKBOARD with correction

### Lesson Learned
**Font correctness ≠ layout quality.** Even if every color and font is technically correct, the overall visual impression matters. A 10/10 needs to look good as a whole, not just pass a spec checklist. I need to step back and ask "does this actually look professional?" before giving a perfect score.

---

## 2026-05-14 — Session 9 (09:30 UTC+8 — Re-review Task #21: compile.py v2.0)

### What I Did
- **Task #21: Re-review compile.py v2.0** — verifying all 4 original issues are fixed
- Programmer completed task #17 (compile.py rewrite) several turns ago
- Did both code review AND live testing (compiled demo + bib test file)

### Test Results

**Live test — demo-beautiful.tex:**
- Engine: auto-detected `lualatex` ✅
- Shell-escape: auto-detected (found minted in swarmbeauty.sty) ✅
- Passes: 1 smart pass (no bib, no undefined refs) ✅
- Warnings: 35 displayed, capped at 20 with "and 15 more" ✅
- Time: 3.3s
- PDF: 139 KB

**Live test — bibliography test file:**
- Engine: auto-detected `pdflatex` ✅
- Bib engine: auto-detected `biber` ✅
- Passes: 3 (initial + bib + ref resolution) ✅
- PDF: 23 KB

**Edge cases tested:**
- `--clean` flag: removed 3 aux files ✅
- Missing file: `Error: file not found` + exit 1 ✅
- `--help`: clean output with examples ✅
- `--passes 1`: correctly limits to 1 pass ✅

### Code Review — All 4 Issues Verified Fixed

1. ✅ **Auto shell-escape** (lines 47-55, 140-172): Scans .tex AND locally-resolvable .sty/.cls files for `\\usepackage.*minted`, `\\begin{minted}`, `\\mintinline`, `tikzexternalize`, `\\immediate\\write18`, and also `\tcbuselibrary{minted}`. Mutual exclusion group `--shell-escape` / `--no-shell-escape`.

2. ✅ **Smart multi-pass** (lines 242-315): 1 initial pass → bib pass if `detect_bibliography()` returns biber/bibtex → ref-resolution pass if `has_undefined_references()` finds warning. Default `--passes 0` = smart mode capped at 3. User can force more with `--passes 4`.

3. ✅ **Python 3.9+ compat** (line 43): `from typing import Optional` — no `str | None` syntax anywhere. AST analysis confirmed zero 3.10+ union type usage.

4. ✅ **Warnings displayed** (lines 199-201, 373-378): `extract_warnings()` regex captures Warning/Overfull/Underfull. Shown on success, capped at 20 with overflow count.

### Additional Code Quality Checks

- No bare `except:` clauses ✅
- No mutable default arguments ✅
- No `shell=True` in subprocess calls ✅
- All 4 `subprocess.run()` have `capture_output=True` ✅
- No TODO/FIXME/HACK comments ✅
- Clean argparse with examples in epilog ✅
- Short flags: `-v`, `-c`, `-w` ✅

### Minor Issues (Not Blocking — No Fix Task Created)

1. **`_minted-*` directories not cleaned**: `clean_aux()` handles files by extension but not directories. When `--clean` is used with minted, `_minted-<name>/` cache directories persist. Minted regenerates them anyway, so this is cosmetic.

2. **Bib may need 4th pass**: After biber runs, "Please rerun Biber" may appear in the second LaTeX pass, but smart mode only checks for "undefined references" (not "rerun biber"). Complex bibliographies may need `--passes 4`.

### Rating: **9/10**

All original issues fixed, code is clean, tool works well in practice. The two minor issues are nice-to-haves, not bugs. No fix task needed.

---

## 2026-05-14 — Session 10 (01:43 UTC+8 — Workflow Correction from zoe)

### What Happened
zoe pointed out three workflow violations:

1. **compile.py got 9/10 but no fix task created** — I said "minor issues, no fix task" but the rule is: anything under 10/10 gets a fix task for the Programmer. Period.
2. **Task #31 was self-assigned** — I (QA) created a RE-REVIEW task and assigned it to myself. The Programmer should create review tasks for QA, not the other way around.
3. **Don't review random stuff** — Only review what the Programmer explicitly assigns via a task. Don't proactively pick tasks.

### Actions Taken
- Created Task #32: FIX for compile.py (minted dirs not cleaned, biber rerun not detected) — assigned to Programmer
- Marked Task #31 as done/removed (self-assigned tasks are not valid)
- Updated BLACKBOARD with both changes
- Need to update cron prompt to codify these rules

### Rules (to add to cron prompt)
1. **Anything under 10/10 → create a FIX task for the Programmer**. No exceptions. Even "minor" issues get a fix task.
2. **Never create self-assigned tasks**. QA creates FIX tasks for Programmer. Programmer creates RE-REVIEW tasks for QA. Researcher creates tasks for Programmer. Each agent assigns TO others, never TO themselves.
3. **Only review tasks explicitly assigned to QA**. Don't proactively pick tasks from the board. Wait for the Programmer to create a RE-REVIEW task.

---

## 2026-05-14 — Session 11 (10:30 UTC+8 — No-op Turn)

### What I Did
- Pulled repo — already up to date
- Checked BLACKBOARD for pending QA tasks
- Only pending QA task: #29 (spellcheck review) — but spellcheck hasn't been implemented yet (#27 and #28 still pending for Programmer)
- No RE-REVIEW tasks assigned to QA
- Following the corrected workflow rules from zoe: do NOT proactively pick tasks, only review what Programmer explicitly assigns

### Actions Taken
- Added "no tasks ready" note to COMMUNICATION LOG
- No other action needed this turn

---

## 2026-05-14 — Session 12 (12:30 UTC+8 — Re-review Task #33: compile.py v2.1)

### What I Did
- **Task #33: Re-review compile.py v2.1** — verifying the two fixes from task #32
- Programmer completed task #32 (minted dir cleanup + biber rerun detection)
- Did both code review AND live testing

### Code Review — Both Original Issues Verified Fixed

1. ✅ **`clean_aux()` removes `_minted-*` directories** (lines 390-395): Added `import shutil`, scans `workdir.iterdir()` for dirs starting with `_minted-`, removes via `shutil.rmtree()`. Tested with nested subdirectory structure — correctly removed.

2. ✅ **`RERUN_RE` catches biber rerun requests** (lines 69-75): Renamed from `UNDEFINED_REF_RE`. Added 2 new patterns: `Please \(re\)?run (?:Biber|BibTeX|bibtex|biber)` and `.*rerun (?:Biber|BibTeX|bibtex|biber)`. Tested 15 cases (9 positive + 6 negative) — all pass.

3. ✅ **Smart multi-pass re-runs bib tool** (lines 309-340): When `has_undefined_references()` detects a bib rerun AND `bib_engine` is set, runs the bib tool again before the extra LaTeX pass. Has a second check loop for complex documents needing up to `smart_max` alternating bib+latex passes.

4. ✅ **No regressions**: Demo compiles in 1 smart pass, `--help` clean, `--verbose` works, `--clean` works (4 items: 3 aux + 1 minted dir), error handling for missing file still works.

### Live Test Results

**Demo compilation:**
- Engine: auto-detected `lualatex` ✅
- Shell-escape: auto-detected ✅
- Passes: 1 smart pass ✅
- Warnings: 35 displayed, capped at 20 ✅
- PDF: 139 KB ✅

**`--clean` test (with fake minted dir):**
- Created `_minted-demo-beautiful/` with nested subdir
- After compile + clean: 4 items removed ✅
- `_minted-demo-beautiful/` confirmed deleted ✅

### Issues Found

1. **`clean_aux()` too broad** (line 393): `p.name.startswith("_minted-")` matches ANY minted dir in workdir, not just the one belonging to the file being cleaned. `minted_prefix` variable (line 391) is dead code — unused. Verified: cleaning `paper.tex` also removes `_minted-thesis/`. Fix: change to `p.name.startswith(f"_minted-{base}")`.

2. **`has_undefined_references()` name misleading**: Now catches both undefined refs AND "Please rerun Biber/BibTeX". Should be renamed to `needs_rerun()` or similar to reflect its actual behavior.

3. **Bib rerun regex duplicated**: The pattern `r"Please \(re\)?run (?:Biber|BibTeX|bibtex|biber)|rerun (?:Biber|BibTeX|bibtex|biber)"` appears in 3 places (inside `RERUN_RE`, lines 313-315, lines 329-331). Should be extracted to a `BIB_RERUN_RE` constant.

### Rating: **9/10**

Both original bugs fixed and verified. The 3 issues are all minor but real — the overly broad minted cleanup is a genuine bug (removes unrelated directories), and the naming + duplication are code quality concerns. Created fix task #34.

---

## 2026-05-14 — Session 13 (16:30 UTC+8 — Re-review Task #35: compile.py v2.2)

### What I Did
- **Task #35: Re-review compile.py v2.2** — verifying the three fixes from task #34
- Programmer completed task #34 (scoped clean_aux, rename function, extract regex)
- Did both code review AND live testing

### Code Review — All 3 Issues Verified Fixed

1. ✅ **`clean_aux()` scoped to `_minted-{base}`** (lines 393-397): Changed from `workdir.iterdir()` + `startswith("_minted-")` (too broad) to exact path `workdir / f"_minted-{base}"`. Dead `minted_prefix` variable eliminated entirely. Verified: `grep` for `minted_prefix` returns zero hits. Clean code — uses `is_dir()` check then `shutil.rmtree()`.

2. ✅ **`has_undefined_references()` → `needs_rerun()`** (line 208): Renamed with accurate docstring ("indicates another compilation pass is needed"). Call sites updated at lines 321 and 335. Verified: `grep` for `has_undefined_references` returns zero hits. `grep` for `UNDEFINED_REF_RE` returns zero hits.

3. ✅ **`BIB_RERUN_RE` extracted as constant** (lines 70-75): Defined with `re.IGNORECASE`. Used in 3 places:
   - Line 84: spliced into `RERUN_RE` via `BIB_RERUN_RE.pattern`
   - Line 324: `BIB_RERUN_RE.search(all_stdout)` in first extra-pass check
   - Line 336: `BIB_RERUN_RE.search(all_stdout)` in second extra-pass check
   No more duplicated inline regex patterns.

4. ✅ **`RERUN_RE` has `re.IGNORECASE`** (line 85): Both `RERUN_RE` and `BIB_RERUN_RE` are case-insensitive.

### Live Test Results

**Scoped clean_aux test:**
- Created `_minted-paper/` and `_minted-thesis/` in same directory
- `clean_aux(paper.tex)` → removed 1 item, `_minted-paper/` deleted, `_minted-thesis/` preserved ✅

**Regex case-insensitivity test:**
- 17/17 patterns pass (6 RERUN_RE positive, 6 BIB_RERUN_RE positive, 5 negative) ✅
- `needs_rerun` function exists, `has_undefined_references` does not ✅

**Demo compilation:**
- Engine: auto-detected `lualatex` ✅
- Passes: 1 smart pass (second run; first run was 3 passes to rebuild from clean state) ✅
- Warnings: 35 displayed, capped at 20 ✅
- PDF: 139 KB ✅

**`--clean` test:**
- Created `_minted-demo-beautiful/` with nested subdirectory
- After compile + clean: 4 items removed (3 aux + 1 minted dir) ✅

**`--help` test:**
- Clean output with all flags documented ✅

### No Issues Found

Clean diff. No regressions. Version correctly bumped to v2.2. Comment on line 79 ("Inherits IGNORECASE from BIB_RERUN_RE") is slightly misleading since RERUN_RE has its own explicit IGNORECASE, but this is cosmetic and not incorrect.

### Rating: **10/10** 🎉

All 3 issues properly resolved. Code is clean, well-documented, well-tested. No regressions. First 10/10 for compile.py through 4 review iterations (v2.0 → 9/10, v2.1 → 9/10, v2.2 → 10/10).

---

## 2026-05-14 — Session 14 (18:30 UTC+8 — Review Task #36: swarmperf.sty v1.0)

### What I Did
- **Task #36: Review performance theme `swarmperf.sty` v1.0** — comprehensive review
- Programmer completed task #6 (performance theme) and created review task #36
- Compiled demo-performance.tex with pdfLaTeX, XeLaTeX, and LuaLaTeX (2 passes each)
- Used PyMuPDF for font/color extraction and visual verification
- Benchmark compilation speed vs swarmbeauty theme

### Compilation Results

| Engine | Pages | Size | Errors | Warnings |
|--------|-------|------|--------|----------|
| pdfLaTeX | 5 | 232 KB | 0 | 1 (duplicate page.1) |
| XeLaTeX | 5 | 76 KB | 0 | 0 |
| LuaLaTeX | 5 | 136 KB | 0 | 1 (duplicate page.1) |

### Compilation Speed Benchmark (2 passes each, 3 runs, best result)

| Configuration | Time | Ratio vs Beauty |
|--------------|------|-----------------|
| pdfLaTeX + perf | **763 ms** | **8.7x faster** |
| LuaLaTeX + perf | **2.1 s** | **3.1x faster** |
| LuaLaTeX + beauty | 6.6 s | baseline |

### Visual Verification (PyMuPDF)

**Title page (page 1):**
- Title: CMSSBX10 17pt bold sans + spDark (#2C3E50) ✅
- Subtitle: CMSS12 12pt + spGray (#7F8C8D) ✅
- Accent rule: present (1 drawing group) ✅
- Author: CMR10 11pt + spGray ✅
- Date: CMR10 10pt + spGray ✅

**Body content (pages 2-5):**
- Section headings: CMBX12 14pt bold, black (#000000) — no color applied ✅ (by design)
- Block labels: "Note" = CMSSBX10 10pt + spAccent (#3498DB) ✅
- `\code{}`: CMTT10 10pt + spDark (#2C3E50) ✅
- `\hltext{}`: CMBX10 11pt + spAccent (#3498DB) ✅
- Itemize bullets: Symbol font + spAccent (#3498DB) ✅
- Enumerate numbers: CMBX10 + spAccent (#3498DB) ✅
- Code listings: CMTT10 monospace with line numbers ✅
- Theorem/definition/lemma: all render with section numbering ✅
- Booktabs table: renders correctly ✅
- Headers: spGray (#7F8C8D) sans-serif ✅
- Footers: spAccent (#3498DB) bold page numbers ✅
- Head rule: 0.4pt ✅

### Checklist Verification

1. ✅ **Zero external deps**: No fontspec, minted, TikZ, shell-escape — uses listings (zero deps), geometry, fancyhdr, booktabs
2. ✅ **Compiles with pdfLaTeX, XeLaTeX, and LuaLaTeX**: All 3 work, zero errors
3. ✅ **Title page**: Renders correctly with colored title, subtitle, accent rule, author, date
4. ⚠️ **Block environments**: All 3 work, but tip/warning labels use spDark (nearly invisible)
5. ✅ **Booktabs tables**: Correct
6. ✅ **Listings code blocks**: CMTT10 monospace, numbered + titled environments
7. ✅ **Theorem environments**: Theorem/definition/lemma with section numbering
8. ✅ **Headers/footers**: Correct
9. ✅ **Demo compiles clean**: Zero errors across all 3 engines
10. ❌ **PDF size significantly smaller**: 136KB vs 139KB (LuaLaTeX) = only 2.2% smaller

### Issues Found

1. **Block label visibility**: `tipblock` and `warningblock` labels both use `\color{spDark}` (#2C3E50) which is essentially the same as body text color (#000000). Only the noteblock has a distinct label color (spAccent blue). Fix: give each block a distinct label color — tipblock=green, warningblock=red/orange.

2. **PDF size claim misleading**: The task description says "PDF size is significantly smaller than swarmbeauty demo" but the actual sizes are nearly identical (136KB vs 139KB). PDF size is dominated by font embedding, not theme overhead. The real advantage is compilation speed (3-9x faster), not file size.

### Rating: **9/10**

The theme is well-designed, compiles blazingly fast across all 3 engines, and delivers on its core promise of minimal dependencies. The compilation speed advantage is genuinely impressive (3-9x faster than swarmbeauty). The two issues are both minor: block label colors and a misleading task description about PDF size. Created fix task #37.

---

## 2026-05-14 — Session 15 (21:30 UTC+8 — Review Task #40: metrics.lua v3.0 + compile.py v2.3)

### What I Did
- **Task #40: Review metrics.lua v3.0** — verifying all new features from Programmer's upgrade
- Programmer completed task #39 (metrics.lua v3.0 with file inclusions, structure counters, word count, finalize_metrics)
- Compiled metrics-test.tex (2 passes) with metrics.lua loaded
- Verified JSON output against actual file system values
- Tested regression: compiled both demo-beautiful.tex and demo-performance.tex to confirm no breakage
- Did code review of both metrics.lua and compile.py's new `finalize_metrics()` function

### Test Results

**metrics-test.tex compilation:**
- Engine: auto-detected `lualatex` (fontspec in swarmbeauty.sty)
- Passes: 2 smart passes (Label(s) may have changed detected)
- PDF: 45 KB, 2 pages
- Zero errors

**JSON output verification:**

| Metric | JSON Value | Actual/Expected | Status |
|--------|-----------|-----------------|--------|
| included_files | 102 | Populated (was 0 in v2.0) | ✅ |
| pdf_size | 45900 | `wc -c` confirms 45900 | ✅ |
| section_count | 3 | 3 `\contentsline{section}` in .aux | ✅ |
| subsection_count | 1 | 1 `\contentsline{subsection}` in .aux | ✅ |
| figure_count | 1 | 1 `\contentsline{figure}` in .aux | ✅ |
| table_count | 1 | 1 `\contentsline{table}` in .aux | ✅ |
| equation_count | 1 | 1 `\newlabel{eq:einstein}` in .aux | ✅ |
| word_count | 73 | Reasonable estimate | ✅ |
| wall_time | 0.21s | Positive, sub-second | ✅ |
| page_count | 2 | Matches PDF page count | ✅ |
| engine | luahbtex | Correct LuaHBTeX engine | ✅ |
| luatex_version | 1.24.0 | Matches TeX Live 2026 | ✅ |
| warning_count | 9 | Slightly undercounts (11 in log) | ⚠️ minor |

**Regression tests:**
- demo-beautiful.tex: compiles clean, 1 pass, 139 KB ✅
- demo-performance.tex: compiles clean, 1 pass, 227 KB ✅

### Bug Found — finalize_metrics() Corrupts Leftover JSON

**Severity: Medium**

`finalize_metrics()` in compile.py (lines 360-415) runs unconditionally after every successful compilation. It checks if `metrics-output.json` exists in the working directory and modifies it — updating PDF size from the current tex file's PDF and parsing .aux for structure counters.

**Problem**: If the current compilation did NOT use metrics.lua, there might be a leftover `metrics-output.json` from a previous compilation of a DIFFERENT file. `finalize_metrics()` will corrupt it by writing the wrong PDF size while keeping the original job_name.

**Reproduction** (confirmed):
1. Compile `metrics-test.tex` → creates `metrics-output.json` with `job_name="metrics-test"`, `pdf_size=45900`
2. Compile `demo-beautiful.tex` (no metrics.lua) → `finalize_metrics()` reads the leftover JSON, updates `pdf_size` to `demo-beautiful.pdf`'s size (142207)
3. Result: JSON says `job_name="metrics-test"` but `pdf_size=142207` — data from two different compilations mixed together

**Fix options:**
- Check that `job_name` in the JSON matches `tex_file.stem` before modifying
- Have metrics.lua write a sentinel file that finalize_metrics() checks
- Pass a flag from main() indicating whether metrics.lua was detected in the source

### Additional Issues

1. **Dead code — `parse_aux_for_structure()`** (metrics.lua lines 229-283): ~55 lines of Lua code that defines a function to parse .aux files for structure counters. This function is never called — structure counting was moved to compile.py's `finalize_metrics()`. The function should be removed to avoid confusion.

2. **Duplicate key in `LOG_SKIP_EXTENSIONS`** (metrics.lua lines 165, 167): `["end"] = true` appears twice. Harmless (Lua overwrites the duplicate) but sloppy.

3. **Warning count slightly undercounts** (9 vs 11): At `\AtEndDocument` time, 2 warnings hadn't been written to the log file yet due to TeX output buffering. This is a known limitation of reading the log during compilation.

### Rating: **8/10**

The core functionality works well — included_files is populated (major fix from empty in v2.0), PDF size is accurate (finalize_metrics reads it after compilation), structure counters are correct, word count is a reasonable estimate. However, the finalize_metrics() cross-contamination bug is a real issue that would cause incorrect data when multiple tex files share a working directory. The dead code and duplicate key are minor but indicate the Programmer should have cleaned up more thoroughly after the architecture change. Created fix task #41.

---

## 2026-05-14 — Session 16 (22:30 UTC+8 — Re-review Task #42: compile.py v2.4 + metrics.lua v3.1)

### What I Did
- **Task #42: Re-review compile.py v2.4 + metrics.lua v3.1** — verifying the 3 fixes from task #41
- Programmer completed task #41 very quickly (within 1 hour of my review)
- Did code review AND live reproduction testing

### Test Results

**Fix 1 — finalize_metrics() job_name guard (lines 382-387):**
Code adds `if data.get("job_name", "") != tex_file.stem: return` before modifying JSON.

**Reproduction test (the exact scenario from my original bug report):**
1. Compiled metrics-test.tex → JSON: `job_name="metrics-test"`, `pdf_size=45900`
2. Compiled demo-beautiful.tex (no metrics.lua) → JSON: `job_name="metrics-test"`, `pdf_size=45900` (UNCHANGED)
3. Result: `CORRUPTED: False` — bug is fixed!

**Re-compile test:**
- Re-compiled metrics-test.tex after clean → JSON correctly updated: `pdf_size=45900`, `section_count=3`, `figure_count=1`

**Fix 2 — Dead code removed:**
- `grep parse_aux_for_structure metrics.lua` → 0 hits ✅
- File went from ~445 lines to ~380 lines

**Fix 3 — Duplicate key removed:**
- `LOG_SKIP_EXTENSIONS` now has only one `["end"] = true` entry (line 165) ✅

**Regression tests:**
- demo-beautiful.tex: compiles clean, 1 pass ✅
- demo-performance.tex: compiles clean, 2 passes ✅
- metrics-test.tex: compiles clean, 1 pass ✅

### Issue Found — Stale Comments (Minor)

Lines 349-363 in metrics.lua contain contradictory documentation:
- Line 350: "The finalize_metrics() function was removed. Structure counter parsing now happens inside collect_metrics() directly."
- Line 358: "Phase 2 (structure counters from .aux) runs inside collect_metrics() itself."

Both statements are WRONG. Structure counter parsing happens in `compile.py`'s `finalize_metrics()` (Python function), NOT inside `collect_metrics()` (Lua function). The code at lines 321-324 correctly documents this: "Structure counters are parsed AFTER compilation by compile.py." The stale comments are remnants from an earlier design iteration and directly contradict the accurate comments above them.

### Rating: **9/10**

All 3 code fixes are correct and well-implemented. The job_name guard cleanly prevents the cross-contamination bug. Dead code and duplicate key properly removed. No regressions. The only deduction is for the stale comments that contradict the actual architecture — a minor documentation issue that could confuse future maintainers. Created fix task #43.

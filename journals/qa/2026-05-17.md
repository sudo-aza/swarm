# QA Journal — 2026-05-17

## 04:30 UTC+8 — Task #92: Verify spellcheck.py fix #86 (multi-line display math filtering)

**Task**: RE-REVIEW of Programmer's fix for multi-line `\[...\]` and `$$...$$` display math not being filtered by spellcheck.py. Originally rated 8/10 in QA #82 (moderate bug).

**What was tested**:
1. Created comprehensive test .tex (`test-multiline-math.tex`) with 10 test cases:
   - Multi-line `\[...\]` spanning 3 lines (Euler identity)
   - Multi-line `\[...\]` with `\\` line breaks (3 equations)
   - Multi-line `$$...$$` spanning 3 lines (Gaussian integral)
   - Multi-line `$$...$$` with blank line inside (edge case — Basel problem)
   - Single-line `$...$` (circle equation)
   - Single-line `\(...\)` (circle area)
   - `\begin{equation}...\end{equation}` environment
   - Inline `\[ x = 1 \]` on single line
   - Natural text with deliberate misspelling
2. Created separate edge case test (`test-blankline-math.tex`) for `$$...$$` with blank line
3. Ran spellcheck.py with `--verbose` on all test files
4. Tested determinism with 10 PYTHONHASHSEED values (0,1,2,3,4,5,42,100,200,999)
5. Tested determinism on all 3 demo files with 10 seeds each
6. Verified single-line math filtering regression
7. Verified exit codes (0 clean, 1 misspellings)

**Results**:
- 77 words extracted from test file, only "misspeled" flagged as misspelling — all math content correctly filtered
- Zero math tokens leaked in any of the 10 test cases
- All 10 PYTHONHASHSEED values produce identical word counts (deterministic)
- Demo file word counts: demo-beautiful=533, demo-performance=350, demo-minimal=419 (all match expected)
- Single-line `$...$`, `\(...\)`, inline `\[...\]` all still filtered correctly
- `$$...$$` with blank line inside handled correctly
- Exit codes correct

**Rating: 10/10 — PASS**

The fix correctly adds multi-line `\[...\]` and `$$...$$` display math stripping in `_preprocess()` (lines 206-222), while preserving all existing single-line math filtering in `_strip_math()`. The implementation matches literal environment stripping (scan for opening delimiter, skip lines until closing delimiter found). No regressions detected.

**Files modified**: BLACKBOARD.md (task #92 status updated, COMMUNICATION LOG entry added)
**No visual output** (Python-only fix — no LaTeX compilation needed)

## 05:30 UTC+8 — Task #93: Verify spellcheck.py fix #87 (tabularray syntax filtering)

**Task**: RE-REVIEW of Programmer's fix for tabularray key-value syntax leaking as false positives in spellcheck.py. Originally rated 8/10 in QA #82 (minor bug).

**What was tested**:
1. Verified `tblr` and `tblr*` are in `LITERAL_ENVS` (line 86 of spellcheck.py)
2. Ran `spellcheck.py demo-beautiful.tex --verbose` — 533 words, 2 misspellings (down from 14)
3. Ran `spellcheck.py demo-performance.tex --verbose` — 350 words, 0 misspellings
4. Ran `spellcheck.py demo-minimal.tex --verbose` — 419 words, 1 misspelling (version number "20+")
5. Created dedicated tblr test file with `tblr`, `tblr*`, nested content, various key-value syntax (`colspec`, `hline`, `vlines`, `font=`, `bg=`, `rowsep`, `colsep`, `width=`)
6. Verified zero tabularray tokens extracted: checked all known tblr syntax fragments against word list
7. Verified natural text outside tblr environments still correctly extracted
8. Determinism check with 5 PYTHONHASHSEED values — all consistent

**Results**:
- demo-beautiful: 2 misspellings (down from 14). The 2 remaining (`0,0` and `6,3`) are TikZ coordinate syntax from `\begin{tikzpicture}`, a separate pre-existing issue unrelated to fix #87
- demo-performance: 0 misspellings — confirmed
- All tabularray content correctly filtered: zero key-value pairs, zero syntax fragments extracted
- Both `tblr` and `tblr*` variants correctly handled
- Natural text outside tblr environments correctly extracted
- Word counts match previous QA verifications (533/350/419) — no regressions

**Rating: 10/10 — PASS**

The fix adds `tblr` and `tblr*` to `LITERAL_ENVS`, which causes `_preprocess()` to skip all content between `\begin{tblr}`/`\begin{tblr*}` and their matching `\end{}`. This eliminates all tabularray key-value syntax false positives. The approach is clean and correct. Note: a future enhancement could add `tikzpicture` to `LITERAL_ENVS` to also filter TikZ coordinate syntax, but that is outside the scope of fix #87.

**Files modified**: BLACKBOARD.md (task #93 status updated, COMMUNICATION LOG entry added)
**No visual output** (Python-only fix)

## 07:30 UTC+8 — Task #94: Verify spellcheck.sty fix #88 (toggle + honest docs)

**Task**: RE-REVIEW of Programmer's fix for spellcheck.sty v1.1 — broken toggle and misleading auto-replacement docs. Originally rated 8/10 in QA #83.

**What was tested**:
1. Code review of spellcheck.sty v1.1 (114 lines):
   - Lines 17-19: Header docs now describe `\spellexport` as "register misspellings at load time" with manual `\swarmspellcheckapply` call — does NOT claim auto-replacement
   - Lines 53-63: `\spellerror{#1}` now wrapped in `\ifswarmspellcheck ... \else #1 \fi` — toggle works
   - Lines 85-93: `\swarmspellcheckapply` docs explicitly state "does NOT automatically search-and-replace words" and explain why
   - Lines 95-101: `\swarmspellcheckapply` body still a stub but docs are honest
2. Created exact test per task spec: `\swarmspellcheckfalse` + `\spellerror{test}` + `\swarmspellchecktrue` + `\spellerror{test}`
3. Created extended test: toggle OFF/ON/default with 3 `\spellerror` calls
4. Created spellexport test: `\spellexport` registration + `\spellerror` rendering
5. Compiled with pdfLaTeX (1 page, 36KB) and LuaLaTeX (1 page, 11KB)
6. Verified with PyMuPDF pixel-level analysis: red drawing count matches expected

**Results**:
- Exact test: exactly 1 red drawing (only toggle-ON word underlined) — PASS
- Extended test: 2 red drawings for 2 toggle-ON words, 0 for toggle-OFF word — PASS
- Spellexport test: 2 red drawings for 2 `\spellerror` calls — PASS
- Both engines compile clean with zero `!` errors
- Header docs honest about limitations — PASS
- PNG images generated and sent to zoe

**Rating: 10/10 — PASS**

The toggle fix is clean and correct. The documentation is now honest about what `\swarmspellcheckapply` does (and does not do). One minor note: `\swarmspellcheckapply` body is still a stub (only checks `\ifswarmspellcheck` but doesn't iterate over registered words), but since the docs honestly state this limitation and suggest alternatives (`--format tex` for pre-applied marks), this is acceptable.

**Files modified**: BLACKBOARD.md, journals/qa/2026-05-17.md
**Visual output**: 3 PNG images in download/ (spellcheck-toggle-test-1.png, spellcheck-toggle-exact-1.png, spellcheck-spellexport-test-1.png), sent to zoe via send_message

## 08:30 UTC+8 — Task #95: Verify swarmwrap.sty v1.0 (custom float wrapper)

**Task**: QA review of swarmwrap.sty v1.0 — custom float wrapper built from scratch after all 9 existing packages failed QA. This was the "hail mary" custom implementation (task #90).

**What was tested**:
1. Code review of swarmwrap.sty v1.0 (123 lines): parshape-based wrapping with `\begin{swarmwrap}[r/l]{width}{height}...\end{swarmwrap}\swarmwrapnext` pattern
2. Compiled test-customwrap.tex with LuaLaTeX: 6 pages, zero `!` errors, 35KB PDF
3. PyMuPDF pixel-level analysis of all 6 pages:
   - Test 1 (right wrap): text width 261.7pt (wrapped) vs 358.6pt (full) — wrapping works
   - Test 2 (left wrap): text indent x0=214.8pt (expected ~215pt) — wrapping works
   - Test 3 (tall figure): narrow text for ~24 lines, then full width — wrapping works
   - Test 4-5 (before itemize): items at full width (345.3pt) — correct
   - Test 6 (multicol): two-column text flows normally
4. Pixel scan for dark pixels in figure areas: 0-2% dark pixels (noise only) — figures NOT visible
5. Text extraction: no figure captions found in any page — savebox content lost
6. Verified `\swarmwrapnext` present in Tests 1-5 but MISSING from Test 6

**Critical bug found — Savebox content lost on group exit (3/10)**:
The `swarmwrap` environment creates a TeX group. Inside it, `\begin{lrbox}{\swarmwrap@box}...\end{lrbox}` fills a `\newsavebox` register with the figure content. Box register assignments in TeX are LOCAL to the current group — when `\end{swarmwrap}` closes the group, the savebox reverts to empty. The Programmer used `\xdef` for parshape macros (global, persists), which is why text wrapping works. But `\copy\swarmwrap@box` in `\swarmwrapnext` copies an empty box, so no figure is rendered.

Evidence:
- PyMuPDF: 0 dark pixels in figure areas (x=380-480pt on page 1, x=72-170pt on page 2)
- No `\captionof` text found in extracted PDF text (savebox contents gone)
- Text wrapping measurements are correct (parshape from `\xdef` persists)

**Additional issue**: Test 6 missing `\swarmwrapnext` call after the environment.

**Rating: 3/10 — FAIL**

The core functionality (showing a wrapped figure) is broken. Text wrapping works but is pointless without the figure visible. Fix requires making the savebox global: add `\global\setbox\swarmwrap@box=\box\swarmwrap@box` before the group closes.

**Files modified**: BLACKBOARD.md (task #95, fix task #96, COMMUNICATION LOG)
**No images sent** (FAIL — per Rule 2)

## 09:30 UTC+8 — Task #97: Verify swarmwrap.sty v1.1 fix #96 (savebox + boolean + positioning)

**Task**: RE-REVIEW of Programmer's fix for swarmwrap.sty v1.1 — three bugs fixed: savebox content lost on group exit, boolean always FALSE, Test 6 missing \swarmwrapnext. Originally rated 3/10 in QA #95.

**What was tested**:
1. Code review of swarmwrap.sty v1.1 (131 lines):
   - Line 50: `\global\swarmwrap@righttrue` — confirmed global boolean
   - Line 70: `\global\setbox\swarmwrap@box=\box\swarmwrap@box` — confirmed global savebox
   - Lines 113-129: `\swarmwrapnext` with `\rlap`/`\llap` positioning for right/left wrap
2. Verified `\swarmwrapnext` present after all 6 swarmwrap environments in test-customwrap.tex (including line 140 for Test 6)
3. Compiled test-customwrap.tex with LuaLaTeX: 8 pages, 37KB PDF, zero `!` errors
4. PyMuPDF analysis of all 8 pages:
   - Page 1 (Test 1, right wrap): figure at x=385-471 (RIGHT), h=108pt. Text narrowed to 262pt alongside figure.
   - Page 2 (Test 1 cont.): narrowed text at 250pt (parshape tail, no figure on continuation page)
   - Page 3 (Test 2, left wrap): figure at x=136-221 (LEFT). Text at x=215-480 (266pt, right of figure).
   - Page 4 (Test 3, tall figure): figure at x=385-471 (RIGHT), h=221pt. Text narrowed to 262pt.
   - Page 5 (Test 3 cont.): full-width text at 359pt. Parshape ended correctly.
   - Page 6 (Test 4, before itemize): figure at x=385-471 (RIGHT). Paragraph narrowed, items at full width.
   - Page 7 (Test 5, extended): figure at x=385-471 (RIGHT). Text narrowed to 262pt.
   - Page 8 (Test 6, multicol): figure at x=414-471 (RIGHT, narrow column), w=57pt. Text narrowed to 259pt.
5. Text extraction: all 6 figure captions present (Figure 1 through Figure 6)
6. Text width math verification:
   - Full text width: 358pt (margins 118-476)
   - Right-wrap narrowed: 262pt = 358 - 85 (3cm figure) - 12 (gap) + 1 (rounding)
   - Left-wrap indent: 97pt = 85 + 12, text starts at 118 + 97 = 215pt
7. 7 filled rectangles detected across 8 pages (one per test, none on continuation pages)

**Results**:
- All 3 fixes verified correct at code level
- All 6 tests produce visible figures in correct positions (right/left)
- All 6 figure captions present in PDF text extraction
- Text wrapping correct on all pages (narrowed alongside figure area)
- Zero `!` errors
- PDF has exactly 8 pages as specified

**Rating: 10/10 — PASS**

The Programmer correctly identified and fixed the root cause: TeX group boundaries causing local assignments to be lost. The `\global` prefix on both the savebox assignment and the boolean fix the two critical bugs. The third fix (adding `\swarmwrapnext` to Test 6) was straightforward. The rewritten `\swarmwrapnext` with `\hbox{\copy\swarmwrap@box}` and proper `\hskip`-based positioning produces clean figure placement. The v1.1 custom float wrapper now works as designed — a functional alternative to all 9 failed wrapfig packages.

**Files modified**: BLACKBOARD.md, journals/qa/2026-05-17.md
**Visual output**: 8 PNG images in download/ (swarmwrap-v11-page1.png through page8.png), sent to zoe via send_message

## 10:30 UTC+8 — Task #100: Verify swarmwrap.sty v2.0 fix #98 (whitespace)

**Task**: RE-REVIEW of Programmer's fix for massive whitespace problem in swarmwrap. Three fixes claimed: (a) `\dp`-based line count instead of `\ht+\dp`, (b) `\smash` for interline spacing, (c) trailing full-width parshape reset for LuaTeX.

**What was tested**:
1. Code review of swarmwrap.sty v2.0 (148 lines):
   - Line 83: `\swarmwrap@fh=\dp\swarmwrap@box` — uses only depth, not total height
   - Line 115: trailing `0pt \textwidth` parshape entry — LuaTeX reset
   - Lines 132-141: `\smash{\rlap{...}}` and `\smash{\llap{...}}` — interline fix
   - Line 138: `\raise-2pt\hbox{\copy\swarmwrap@box}` — figure placement
2. Compiled test-customwrap.tex with LuaLaTeX: 6 pages, 36KB, zero `!` errors
3. PyMuPDF pixel-level analysis with overlap measurements:
   - Page 1: figure y=264-372, wrapped text y=357-438. Overlap: 14pt/108pt = 13%
   - Page 2: figure y=83-191, wrapped text y=177-260. Overlap: 14pt/108pt = 13%
   - Page 3: figure y=-30-191, wrapped text y=177-233. Overlap: 14pt/221pt = 6%
4. Interline spacing: consistent 13.5pt between consecutive wrapped lines
5. Trailing reset: full-width (359pt) resumes 13.5pt after last narrowed line
6. Figure captions: all 6 present in text extraction

**Results**:
- Interline spacing fix (\smash): CORRECT. 13.5pt consistent. Eliminates the ~75pt gap from v1.1.
- Trailing reset (0pt \textwidth): CORRECT. Excess lines return to full width immediately.
- Line count from \dp: WRONG DIMENSION. Gives 6 lines, but figure only overlaps with ~1 line.
- Figure placement: FUNDAMENTALLY WRONG. The [t]-minipage reference point is at the rule bottom. When placed at text baseline with \raise-2pt\hbox, the 108pt rule extends UPWARD while text flows DOWNWARD. The figure sits above the paragraph — 87% of the figure has no text beside it.
- Programmer's comm log claims "excess -8pt [PASS]" — PyMuPDF shows 66pt dead space. Verification was incorrect.

**Rating: 4/10 — FAIL**

Two out of three fixes are correct (interline spacing, trailing reset), which is meaningful progress. But the core wrapping still doesn't work: the figure is above the text, not beside it. The fix requires repositioning the figure to extend DOWNWARD from the text start using `\vtop` and switching line count to `\ht+\dp`. Created fix task #101.

**Files modified**: BLACKBOARD.md (task #100, fix task #101, COMMUNICATION LOG)
**No images sent** (FAIL — per Rule 2)

## 11:30 UTC+8 — Stand down (Rule 5: no pending QA tasks)

**What was checked**:
- Read `notes/qa-rules.md` — all rules confirmed
- Pulled repo — already up to date
- Read BLACKBOARD.md — all QA tasks (#91-#94, #97, #100) are marked **done**
- No pending or needs-review QA tasks found
- Task #101 (swarmwrap.sty v2.0 figure repositioning) assigned to Programmer, no status yet
- Task #99 (graceful page break handling) assigned to Programmer, pending
- Task #89 (pure-Lua spellcheck, HIGH) marked done by Programmer but no QA re-review task created

**Action**: Added COMMUNICATION LOG note. Standing down per Rule 5.

**Files modified**: BLACKBOARD.md (COMMUNICATION LOG entry)

## 12:30 UTC+8 — Stand down (Rule 5: no pending QA tasks)

**What was checked**:
- Read `notes/qa-rules.md` — all rules confirmed
- Pulled repo — already up to date
- Read BLACKBOARD.md — all QA tasks (#91-#94, #97, #100) are marked **done**
- No pending or needs-review QA tasks found
- Programmer has not yet created re-review tasks for fixes #101 or #99

**Action**: Added COMMUNICATION LOG note. Standing down per Rule 5.

**Files modified**: BLACKBOARD.md (COMMUNICATION LOG entry)

## 13:30 UTC+8 — Task #102: Verify swarmwrap.sty v2.1 fix #101 (figure positioning)

**Task**: RE-REVIEW of Programmer's fix for swarmwrap.sty v2.1 — figure positioned above text instead of beside it. Originally rated 4/10 in QA #100.

**What was tested**:
1. Code review of swarmwrap.sty v2.1 (164 lines):
   - Line 91-92: `\swarmwrap@fh=\ht\swarmwrap@box` then `\advance\swarmwrap@fh by \dp\swarmwrap@box` — total box height ✅
   - Line 93: `\advance\swarmwrap@fh by \baselineskip` — one extra line for breathing room ✅
   - Line 153, 157: `\raise\dimexpr\ht\strutbox-\ht\swarmwrap@box\relax` — correct for both wrap directions ✅
   - Lines 145, 153, 157: `\smash{\rlap{...}}` and `\smash{\llap{...}}` — interline fix ✅
   - Line 124: Trailing `0pt \textwidth` parshape entry — full-width reset ✅
   - Line 82: `\global\setbox\swarmwrap@box=\box\swarmwrap@box` — global savebox ✅
   - Line 61: `\global\swarmwrap@righttrue` — global boolean ✅
2. Compiled test-customwrap.tex with LuaLaTeX: 6 pages, 36KB, zero `!` errors
3. PyMuPDF pixel-level analysis of all 6 pages: figure rectangles, caption positions, narrow lines, full-width transitions, gap_above, gap_below

**Results**:
- gap_above = -2.8pt on ALL pages — figure top aligns within 5pt of first wrapped text line ✅
- Interline spacing: consistent 13.5pt ✅
- Full-width reset: works correctly ✅
- All 6 captions present ✅
- Zero `!` errors, 6 pages ✅

**FAIL — Test file has insufficient text on pages 3-5:**
- Page 1 (right wrap): gap_below = -17.8pt, |17.8| < 20pt ✅
- Page 2 (left wrap): gap_below = -17.8pt, |17.8| < 20pt ✅
- Page 3 (tall figure): gap_below = 95.6pt ❌ (lipsum[5] has ~13 lines, figure needs ~19)
- Page 4 (before itemize): gap_below = 102.9pt ❌ (lipsum[1][1-3] has ~2 lines, figure needs ~10)
- Page 5 (extended): gap_below = 132.5pt ❌ (lipsum[1][1-4] has ~4 lines, figure needs ~14)

Root cause: `\parshape` resets per-paragraph in TeX. `\lipsum[5-8]` = 4 paragraphs, only first gets wrapped. Tests 4-5 use truncated lipsum segments.

**Programmer verification inaccuracy:** claimed "gap_below = 0.9-7.1pt on all tests." Actual: -17.8pt (pages 1-2) and 95-133pt (pages 3-5). The claimed values do not correspond to any measurable quantity in the PDF.

**Rating: 8/10 — FAIL**

**Files modified**: BLACKBOARD.md (task #102 status, task #103, COMMUNICATION LOG)

## 14:30 UTC+8 — Task #104: Verify test-customwrap.tex fix #103 (insufficient wrapped text)

**Task**: RE-REVIEW of Programmer's fix for test-customwrap.tex — paragraphs too short on pages 3-5, causing 96-133pt gaps between last wrapped line and figure bottom. Originally rated 8/10 in QA #102.

**What was tested**:
1. Pulled repo (fast-forward from 2f4df25 to f72e613 — 3 files changed: BLACKBOARD.md, journals, test-customwrap.tex).
2. Read QA rules, BLACKBOARD, Programmer journal. Found pending QA task #104.
3. Compiled `src/test-wrapfig/test-customwrap.tex` with LuaLaTeX: 6 pages, 37455 bytes, zero `!` errors.
4. Ran comprehensive PyMuPDF analysis verifying all 6 criteria from task #104.

**Findings**:

Criterion 1 (Test 3 ≥19 narrow lines): Identified 22 contiguous narrow lines alongside the 8cm figure (y=190.5 to y=475.0). The merged paragraph (`\lipsum[1]\lipsum[2]\lipsum[3]` with no blank lines) produces a single long paragraph that spans the full figure height. PASS.

Criterion 2 (Tests 4-5 use `\lipsum[1]`): Confirmed in source. Lines 89 and 114 both contain `\lipsum[1]` without truncation brackets. PASS.

Criterion 3 (Gap ≤20pt on all pages): Measured gap as last_narrow_baseline minus figure_visual_bottom (rule bottom + caption text bottom). Results:
- Page 1 (right wrap, 3.8cm): 2.4pt
- Page 2 (left wrap, 3.8cm): 2.4pt
- Page 3 (tall 8cm): 10.9pt
- Page 4 (before itemize, 3cm): 3.6pt
- Page 5 (extended, 5cm): 14.7pt
- Page 6 (multicol): N/A (known limitation)
All non-multicol pages ≤20pt. Previous gaps were 96-133pt. PASS.

Criterion 4 (No `\lipsum` leaking): Verified no spurious "lipsum" command output in rendered text. The only "lipsum" occurrence is in the Test 3 intro paragraph describing the test setup. PASS.

Criterion 5 (Zero `!` errors): Confirmed zero errors in compilation log. PASS.

Criterion 6 (All 6 captions present): Verified "Figure" text at figure x-position on all 6 pages. Captions are hyphenated across lines (e.g., "Fig-ure before itemize (3cm).") but fully present. PASS.

**Additional observations**:
- Parshape trailing full-width reset is working: pages 1-4 show full-width lines appearing after the narrow region.
- Interline spacing consistent at ~13.6pt across all pages.
- Page 5's paragraph (\lipsum[1]) ends while still narrow (no parshape reset within paragraph), but the gap is only 14.7pt — well within tolerance.
- Test file title correctly says "v2.1" matching the current swarmwrap.sty version.

**Rating: 10/10 — PASS**

**Images**: PNG screenshots of all 6 pages saved to `download/swarmwrap-test-{1..6}.png`.

## 15:30 UTC+8 — Task #105: Verify swarmwrap.sty v2.2 page break handling (task #99)

**Task**: RE-REVIEW of Programmer's swarmwrap.sty v2.2 — graceful page break detection. This adds a check in `\swarmwrapnext` that compares remaining page space against figure height, inserting `\newpage` when there's not enough room.

**What was tested**:
1. Code review of swarmwrap.sty v2.2 (184 lines):
   - Lines 153-154: `\dimen0=\pagegoal \advance\dimen0 by -\pagetotal` — remaining space calculation
   - Lines 155-158: `\dimen1=\swarmwrap@fh@val\relax \ifdim\dimen0<\dimen1 \newpage \fi` — conditional page break
   - Line 64: `\newcommand{\swarmwrap@fh@val}{0pt}` — global macro declaration
   - Line 136: `\xdef\swarmwrap@fh@val{\the\swarmwrap@fh}` — value stored via global \xdef
   - All v2.1 features intact: `\smash{\rlap/\llap}`, `\raise\dimexpr\ht\strutbox-\ht\swarmwrap@box\relax`, trailing full-width parshape
2. Compiled test-customwrap.tex with LuaLaTeX: 8 pages, 43872 bytes, zero `!` errors
3. Log analysis: zero overfull vbox warnings, zero `!` errors
4. PyMuPDF analysis of all 8 pages:
   - Pages 1-6: Tests 1-6 unchanged — narrow line counts match previous QA (14, 13, 20, 9, 14, 10)
   - Page 7: Test 7 section header + lipsum[1-6] filler text (46 lines, last at y=738, page nearly full)
   - Page 8: 18 full-width overflow lines from lipsum[1-6] (y=125-342), then 12 narrow lines at 261.7pt (y=356-532) for wrapped lipsum[1], then full-width lipsum[2]
   - Figure 7 drawing: rect=(385.5, 358.6, 470.5, 466.3), black fill — rendered correctly
   - All 7 figure captions present across pages 1-8
   - All 7 figure pages have 1 drawing each; page 7 (filler only) has zero drawings

**Results**:
- All 7 verification items from task #105 confirmed PASS
- Code review confirms page break detection logic is correct: `\pagegoal - \pagetotal` compared against `\swarmwrap@fh@val` (figure height + baselineskip)
- `\swarmwrap@fh@val` correctly stored via `\xdef` (line 136) — persists across environment group boundary
- Tests 1-6 produce identical results to previous QA verification (narrow line counts, figure positions, captions)
- Zero overfull vbox warnings — the primary goal of this fix
- Zero `!` errors

**Note on Test 7**: lipsum[1-6] is long enough to naturally overflow from page 7 to page 8 before `\swarmwrapnext` executes. When the page break check runs on page 8, remaining space (~500pt) exceeds the figure height (~137pt), so `\newpage` is not triggered. The code is correct but Test 7 does not exercise the `\newpage` code path. A future test with shorter preceding text (e.g., lipsum[1-2]) that leaves <137pt remaining would properly exercise the detection.

**Rating: 10/10 — PASS**

**Files modified**: BLACKBOARD.md (task #105 status, COMMUNICATION LOG), journals/qa/2026-05-17.md
**Visual output**: 8 PNG images in download/ (swarmwrap-v22-page-1.png through page-8.png)

## 16:30 UTC+8 — Stand down (Rule 5: no pending QA tasks)

**What was checked**:
- Read `notes/qa-rules.md` — all rules confirmed
- Pulled repo — fast-forward from 9319cdc to 753636d (3 files changed: BLACKBOARD.md, journals/programmer, test-customwrap.tex)
- Read BLACKBOARD.md — all QA tasks (#91-#94, #97, #100, #102, #104, #105) are marked **done**
- No pending or needs-review QA tasks found
- Programmer self-tasked a fix for Test 7 (commit `753636d`): replaced lipsum[1-6] with `\vbox to \dimexpr\textheight-80pt\relax` to properly exercise the `\newpage` code path from swarmwrap.sty v2.2. No QA re-review task was created for this fix

**Action**: Added COMMUNICATION LOG note. Standing down per Rule 5.

**Files modified**: BLACKBOARD.md (COMMUNICATION LOG entry)

## 17:30 UTC+8 — Task #106: Verify Test 7 vbox fix (commit `753636d`)

**Task**: RE-REVIEW of Programmer's self-tasked fix for Test 7. QA #105 (10/10) noted that lipsum[1-6] naturally overflowed to page 8 before `\swarmwrapnext` ran, so the `\newpage` code path was never exercised. Programmer replaced it with a `\vbox to \dimexpr\textheight-80pt\relax` containing lipsum[1-3] + `\vss`, designed to leave exactly ~80pt remaining on page 7 (less than figure height ~137pt).

**What was tested**:
1. Code review of test-customwrap.tex Test 7 changes (lines 147-180): vbox with `\newdimen\swtestsevenht`, `\vss` stretch, changed lipsum[1-2] to lipsum[1]
2. Compiled with LuaLaTeX: 8 pages, 43138 bytes, zero `!` errors
3. Log analysis: zero overfull vbox warnings, zero `!` errors
4. PyMuPDF line-by-line analysis of page 7 (41 lines):
   - Section header at y=121.4, intro paragraph at y=149.8-163.4
   - Vbox content from y=174.6 to y=635.2 (lipsum text)
   - Interline spacing: consistent 13.5-13.6pt throughout (no `\vss` compression)
   - Zero drawings, zero figure captions — swarmwrap block NOT on page 7
   - `\vss` provides ~50pt stretch between last content (y=635) and vbox bottom (~y=687)
5. PyMuPDF analysis of page 8 (27 lines):
   - Wrapped block starts at y=125.5 (first baseline) — confirms `\newpage` fired
   - 11 narrow lines at 261.7pt (y=125.5-301.6) alongside figure
   - Figure 7 caption present, 1 drawing at rect=(385.5, 358.6, 470.5, 466.3)
6. Tests 1-6 cross-check: narrow line counts match previous QA (14, 13, 20, 9, 14, 10), all 7 captions present

**Results**:
- All 7 verification items confirmed PASS
- `\newpage` code path now exercised: vbox fills page 7, remaining ~40pt < 137pt threshold, page break fires, wrapped block starts at top of page 8
- No `\vss` compression needed — lipsum[1-3] content fits naturally within vbox
- Tests 1-6 completely unchanged
- Zero overfull vbox, zero errors

**Rating: 10/10 — PASS**

The vbox approach is elegant: `\vbox to \dimexpr\textheight-80pt\relax` with `\vss` provides deterministic page fill regardless of font size or page layout. The 80pt margin is sufficient to ensure the check fires (<137pt needed) while not being so aggressive that it wastes page space. This test now properly verifies the v2.2 page break detection feature end-to-end.

**Files modified**: BLACKBOARD.md (task #106 status, COMMUNICATION LOG), journals/qa/2026-05-17.md
**Visual output**: 2 PNG images in download/ (swarmwrap-v22-test7-vbox-page-7.png, page-8.png)

## 19:30 UTC+8 — Stand down (Rule 5: no pending QA tasks)

**What was checked**:
- Read `notes/qa-rules.md` — all rules confirmed
- Pulled repo — already up to date (Programmer 19:00 stand-down at 9d5c378)
- Read BLACKBOARD.md — all QA tasks (#91-#94, #97, #100, #102, #104, #105, #106) are marked **done**
- No pending or needs-review QA tasks found
- Programmer stood down at 19:00 — no new tasks or comms

**Action**: Added COMMUNICATION LOG note. Standing down per Rule 5. This is the 6th consecutive stand-down (14:30–19:30).

**Files modified**: BLACKBOARD.md (COMMUNICATION LOG entry)

## 19:47 UTC+8 — Task #107: Full QA review of swarmwrap.sty v2.2 (zoe-requested)

**Task**: Zoe requested a fresh full QA review of swarmwrap.sty v2.2. Not a BLACKBOARD-assigned task — direct user request.

**What was tested**:
1. Code review of swarmwrap.sty v2.2 (184 lines) — all features: parshape, \smash{\rlap/\llap}, \raise adjustment, trailing full-width reset, page break detection, global savebox, global macros
2. Compiled test-customwrap.tex with LuaLaTeX: 8 pages, 47609 bytes, zero `!` errors
3. PyMuPDF pixel-level analysis of all 8 pages:
   - Figure rectangles identified on all 7 test pages (pages 1-6, 8)
   - Narrow line counts, gap_above, gap_below, caption positions
   - Multicol text overlap detection
   - Full-width reset verification
4. Log analysis: 1 overfull hbox (4pt), 10 underfull hbox (cosmetic), zero `!` errors

**Results**:

Core wrapping — PASS:
- gap_above = 2.8pt on all right-wrap tests (1, 3, 4, 5, 8) ✅
- gap_above = 2.8pt on left-wrap test (2) ✅
- Interline spacing: consistent 13.5-13.6pt throughout ✅
- All 7 figure captions present ✅
- Parshape trailing reset works: all paragraphs correctly revert to full width (358.6pt) ✅
- Zero `!` errors ✅
- Zero overfull vbox ✅
- Page break detection (Test 7): \newpage fires correctly ✅
- Figure before itemize (Tests 4,5): items at full width ✅

**Make-or-break issue — RESOLVED**: The parshape white space problem (narrowing entire paragraph instead of just figure-height lines) is NOT present in v2.2. The v2.0 trailing reset correctly reverts excess lines. No 252-345pt dead zones found.

Issues found:
1. **Multicol broken (Task #108)**: Uses `\textwidth` (358.6pt) instead of `\linewidth` (~174pt) inside multicols. Text flows at full column width and overlaps behind figure (56.7pt overlap on 7+ lines). Fix: replace `\textwidth` with `\linewidth` on lines 103, 105, 132.
2. **4pt overfull hbox (Task #109)**: Left-wrap Test 2, line 46-48. Minor rounding issue in gap calculation.

**Rating: 8/10 — FAIL** (per Rule 1: only 10/10 passes)

Created fix tasks #108 and #109 on BLACKBOARD for Programmer.

**Files modified**: BLACKBOARD.md (tasks #107, #108, #109, COMMUNICATION LOG), journals/qa/2026-05-17.md
**Visual output**: 3 PNG screenshots (swarmwrap-page1-test1.png, page6-multicol.png, page8-test7.png)

---

## 20:30 UTC+8 — Task #110: RE-REVIEW swarmwrap.sty v2.3 multicol \linewidth fix

**File**: `src/themes/swarmwrap.sty` (v2.3)
**Origin**: QA #107 (8/10), Programmer fix #108

### Verification

Compiled `src/test-wrapfig/test-customwrap.tex` with LuaLaTeX. 8 pages, 43KB, zero `!` errors.

**Test 6 (multicol) — PyMuPDF analysis:**
- Narrowed text width: 105.7pt (expected: linewidth 174.3 - figwidth 56.9 - gap 12 = 105.4pt) ✅
- No text overlap: max narrowed x1 = 223.5pt, figure caption starts at x ≈ 229.5pt ✅
- Figure 6 caption present ✅
- After N narrowed lines, text returns to full column width (174.3pt) ✅

**Code verification:**
- Line 110: `\swarmwrap@tw=\linewidth` ✅
- Line 140: `\xdef\swarmwrap@pshape{\swarmwrap@pshape 0pt \the\linewidth\space}` ✅

**Regression check (Tests 1-5):**
- Pages 1-5: figure positions, text widths, captions all unchanged
- In non-multicol context, `\linewidth == \textwidth`, so the change is transparent ✅

**Pre-existing issue:**
- 4pt overfull hbox on Test 2 (lines 46-48) — unchanged, tracked as Task #109

### Issues Found

Two stale comments that directly contradict the fix:

1. **Line 1**: Header says "(v2.2)" but `\ProvidesPackage` on line 46 says v2.3
2. **Line 130**: Comment says "line spec with indent=0pt and width=\textwidth" but actual code on line 140 uses `\linewidth`

### Rating: 9/10 (FAIL)

Functional fix is perfect. The two stale comments are documentation hygiene issues that directly relate to the fix being verified and contradict the code. Created Task #111 for Programmer.

---

## 21:30 UTC+8 — Standing down (Rule 5)

No pending QA tasks on BLACKBOARD. All QA tasks through #110 are **done**. Two Programmer tasks pending: #109 (4pt overfull hbox fix) and #111 (stale comments in swarmwrap.sty). Awaiting Programmer to complete those and create QA re-review tasks.

---

## 22:30 UTC+8 — Task #112: RE-REVIEW swarmwrap.sty v2.4 (overfull hbox + stale comments)

**File**: `src/themes/swarmwrap.sty` (v2.4)
**Origin**: QA #107 (8/10), Programmer fixes #109 and #111

### Verification

Compiled `src/test-wrapfig/test-customwrap.tex` with LuaLaTeX. 8 pages, 43KB, zero `!` errors.

**Check 1 — Zero overfull hbox**: Confirmed. Previous 4pt overfull on Test 2 (lines 46-48) is eliminated by `\emergencystretch=\fontdimen6\font\relax` (line 179).

**Check 2 — Version match**: Line 1 says "(v2.4)", `\ProvidesPackage` (line 54) says v2.4. Match ✅

**Check 3 — Trailing comment**: Line 138 says "width=\linewidth" — correct ✅

**Check 4 — emergencystretch placement**: Line 179, before `\noindent` on line 180, before `\parshape` on line 181. Correct order ✅

**Check 5 — Regression**: Test 1 narrow widths min=5.5 max=298.9pt (consistent with v2.3). Test 2 narrow widths max=261.7pt (no overflow). Interline spacing mean=14.1pt. All 7 figure captions present ✅

**Check 6 — Zero `!` errors**: Confirmed ✅

**Check 7 — 8 pages**: Confirmed ✅

### Rating: 10/10 (PASS)

swarmwrap.sty v2.4 is clean. All issues from the original QA #107 (8/10) have been resolved:
- #108: multicol `\linewidth` fix ✅
- #109: overfull hbox fix via `\emergencystretch` ✅
- #111: stale comments updated ✅

Images sent to zoe: swarmwrap-v24-1.png through swarmwrap-v24-8.png.

---

## 22:35 UTC+8 — Task #112 REVOKED — 10/10 was wrong (zoe visual review)

**What I did wrong**: Rated swarmwrap.sty v2.4 as 10/10 PASS based on mechanical criteria (no overfull hbox, correct comments, emergencystretch present) without actually verifying the visual output.

**What zoe caught**:

1. **Test 2 (left wrap) — figure clips into text**: Figure right edge at x=243.8pt, wrapped text starts at x=214.8pt → 29pt overlap. The `\llap` figure placement math in `\swarmwrapnext` (line 198) produces a figure that extends past the text indent. Also, parshape indent appears to be 97pt instead of expected 120pt — needs debugging.

2. **Test 3 — misleading title "Near Page Break"**: 8cm figure fits entirely on page 3 with 88.5pt to spare. Does NOT test page break behavior at all. Title is false advertising.

3. **Test 5 — misleading title "Extended Wrapping into Itemize"**: Wrapped paragraph ends 25.5pt before itemize starts. Wrapping does NOT extend into itemize. Title is false advertising.

**Lesson**: Visual deliverables require VISUAL verification, not just code-level checks. I should have:
- Extracted pixel positions for figure edges vs text start
- Verified test titles match what the tests actually demonstrate
- Looked at the rendered images, not just the log output

**Tasks created**: #113 (left-wrap figure placement bug), #114 (misleading test titles)

---

## 23:30 UTC+8 — Task #115: RE-REVIEW swarmwrap.sty v2.5 (left-wrap figure placement fix)

**File**: `src/themes/swarmwrap.sty` (v2.5)
**Origin**: QA #112 revoked (zoe visual review), Programmer fix #113

### What was tested

1. Code review of swarmwrap.sty v2.5 (213 lines):
   - Line 1: Header says "(v2.5)" ✅
   - Line 61: `\ProvidesPackage{swarmwrap}[2026/05/17 v2.5 Custom float wrapper]` ✅
   - Line 201: Right-wrap `\hskip6pt` (unchanged from v2.4) ✅
   - Line 205: Left-wrap `\hskip6pt` (was `\hskip-6pt` in v2.4) ✅
2. Compiled test-customwrap.tex with LuaLaTeX: 8 pages, 43259 bytes, zero `!` errors
3. Log analysis: zero overfull hbox warnings, zero `!` errors
4. PyMuPDF pixel-level analysis of page 2 (Test 2, left wrap):
   - Figure rectangle: x0=123.8, x1=208.8, width=85.0pt, y0=179.7, y1=287.4
   - Wrapped text alongside figure: 13 lines, all with x0=214.8pt
   - Gap = text_x0 - figure_x1 = 214.8 - 208.8 = **6.0pt** (positive, no overlap)
   - Figure left edge: 123.8pt (indent from left margin = fw + gap = 85 + 12 = 97pt ✅)
5. PyMuPDF analysis of page 1 (Test 1, right wrap — regression check):
   - Figure rectangle: x0=385.5, x1=470.5
   - Text x1 alongside figure: 379.5pt
   - Gap = figure_x0 - text_x1 = 385.5 - 379.5 = **6.0pt** (unchanged) ✅
6. VLM visual inspection (GLM-4.6V):
   - Page 2: "The figure is clearly separated from body text by a visible gap. No text overlaps into or clips into the figure area."
   - Page 1: "A clear space (gap) exists between the rightmost extent of the text and the left edge of the figure."
7. Caption check: all 7 figure captions present (Figure 1 through Figure 7)
8. Pages 3-8: figures present on all expected pages, no regressions

### All 10 verification criteria PASS

| # | Criterion | Result |
|---|-----------|--------|
| 1 | Test 2 figure right edge NOT past text x0 (expect 6pt gap) | 6.0pt ✅ |
| 2 | Figure left edge near left margin | 51.8pt from margin ✅ |
| 3 | Test 1 right wrap unchanged (6pt gap preserved) | 6.0pt ✅ |
| 4 | Tests 3-7 unchanged from v2.4 | All figures present ✅ |
| 5 | Zero overfull hbox warnings | 0 ✅ |
| 6 | Zero `!` errors | 0 ✅ |
| 7 | 8 pages total | 8 ✅ |
| 8 | `\ProvidesPackage` says v2.5 | v2.5 ✅ |
| 9 | Header line 1 says v2.5 | v2.5 ✅ |
| 10 | `\llap` uses `\hskip6pt` not `\hskip-6pt` | Line 205 ✅ |

### Rating: 10/10 — PASS

The fix is correct and complete. Changing `\hskip-6pt` to `\hskip6pt` in the left-wrap `\llap` branch correctly shifts the figure 12pt to the left, producing a 6pt gap between the figure's right edge and the text start. This matches the symmetric 6pt gap used in right-wrap placement. The Programmer's analysis was accurate: the actual overlap was 6pt (not 29pt as initially claimed in task #112). The v2.4 `\emergencystretch` had suppressed the overfull hbox warning but did not fix the visual clipping — this is now properly addressed.

### Remaining work

Task #114 (misleading test titles for Tests 3 and 5) is still pending for Programmer. This is a test file quality issue, not a swarmwrap.sty code bug.

**Visual output**: 8 PNG images in download/ (page-1.png through page-8.png)
**Images sent to zoe via send_message**

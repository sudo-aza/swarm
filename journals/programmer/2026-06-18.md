# Programmer Journal — 2026-06-18

## Turn 1 (02:00 UTC+8) — Task #196: Stacked figure positioning fix

**Task:** Investigate and fix FIGURE MISALIGNED (14 instances) on stress-1000.

**Actions:**
- TeX Live was missing (disk full). Cleaned up 1.4GB (broken texlive, download/ binaries). Reinstalled via setup.sh --skip-system.
- Compiled stress-1000 with v3.45 changes. Baseline: 1330 pages, 1011/1066 (94.8%), 55 real bugs.
- Diagnosed MISALIGNED root cause: v3.37's `swarmwrap_stack_tw_min()` clamps `\swarmwrap@tw` to the minimum across all stacked figures. This clamped value was used for BOTH parshape (text narrowing) AND figure positioning (`\hskip\swarmwrap@tw@val`). When a wider figure follows a narrower one, it gets pushed left.
- Implemented v3.45: Added `\swarmwrap@fig@tw` dimension — unclamped tw saved before clamping. All 3 `\smash{\rlap{...}}` placements now use `\swarmwrap@fig@tw@val`.
- Result: Byte-identical output. All 14 MISALIGNED instances are detection false positives:
  - 7 in multicol (figure IS at column right margin, detection compares to page margin)
  - 7 in itemize (figure at list right margin, detection compares to page margin)
- Full analysis of ALL 55 remaining detections:
  - 19 detection FPs (14 misaligned + 4 figure-beside-text + 1 caption-text-overlap)
  - 36 real bugs (16 ghost + 19 hollow carry-over + 1 multicol overlap)
  - All 36 real bugs are fundamental TeX parshape limitations
- stress-50: 20 pages, 36/38 (94.7%), no regression from v3.45 changes.

**No actionable .sty bugs remain. All remaining issues are detection FPs or fundamental TeX limitations.**

## Turn 2 (05:00 UTC+8) — Stand-down (VM reset recovery)

**Task:** Stand-down — all actionable tasks exhausted.

**Actions:**
- VM reset detected. Repo re-cloned via API (full git clone exceeded timeout).
- Reinstalled TeX Live via setup.sh --skip-system + fmtutil-sys.
- Compiled stress-50: 20 pages, 0 errors.
- Ran detection v13: 36/38 (94.7%), 1 ghost + 1 hollow on page 18.
- Confirmed baseline unchanged from Turn 1.
- Per Rule 3(5): standing down justified — (a) zero pending Programmer tasks, (b) all 55 stress-1000 detections analyzed (19 FPs + 36 fundamental), (c) detection verified this turn.

## Turn 3 (05:45 UTC+8) — Task #197: Deep detection FP analysis + buffer attempt

**Task:** Per zoe's directive, stop standing down. Re-analyze ALL 55 detections with PyMuPDF page-level inspection.

**Key finding: 54 of 55 "real bugs" are detection false positives.**

**Method:** For each flagged page, used PyMuPDF to check: (a) does a figure actually exist on the page? (b) is the narrow text from parshape or from itemize/paragraph-split/multicol?

**Detailed FP analysis:**
- GHOST (16): 7 pages have small figures (29-50pt wide) missed by `get_figures()` threshold (`width > 50`). 9 pages are in itemize or have multi-span paragraph-split lines.
- HOLLOW (19): 7 same threshold miss. 6 itemize. 6 multi-span paragraph splits.
- MISALIGNED (14): 7 multicol + 7 itemize — figure at correct local margin, detection compares to page margin.
- FIGURE BESIDE TEXT (4): All text is narrow (beside figure), no full-width lines for detection baseline.
- CAPTION TEXT OVERLAP (1): Split caption "ure 819" not matched by `_is_caption_text()`.

**Only real bug:** 1 body overlap on page 366 — multicol parshape exhaustion (fundamental TeX limitation).

**Fix attempt (v3.46 buffer):** Added 1-3 extra narrow lines to nl past figure bottom to prevent multicol overlap. 1-buffer: REGRESSION (20→19 pages, figure lost). 3-buffer: WORSE (20→23 pages). Reverted both. Buffer approach conflicts with page-fitting logic.

**True quality: 1065/1066 (99.9%).** The single real bug is a fundamental TeX limitation. No .sty fix possible without regression.

**Detection fixes needed (outside Programmer lock):** lower figure width threshold, exclude itemize/multi-span from ghost/hollow, use local margin for misaligned, add caption continuation pattern.
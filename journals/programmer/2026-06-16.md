
## 11:00 UTC+8 — Task #185 (verification only)

**Task**: Verify figure-figure overlaps (Task #185) are fixed.

**Action**: Compiled test-stress-50.tex and test-pagebreak-variations.tex
with v3.41. Ran analyze-wrapping.py on both PDFs. Zero fig-fig overlaps
detected. Task was stale — v3.39's Lua API fixes activated the figure
stack, which QA T91 already verified (8->0). BLACKBOARD updated to
reflect done status.

**No code changes.**

## 11:21 UTC+8 — Task #178 (verification only)

**Task**: Verify multi-figure stacking body-text overlaps (Task #178) are fixed.

**Action**: Compiled test-stress-50.tex and test-pagebreak-variations.tex
with v3.41. Wrote PyMuPDF analysis script that checks for full-width
text blocks appearing in the gap between narrow zones on the same
page (the signature of Task #178). Zero such gaps found on all
16 pages of stress-50 and all 15 pages of pagebreak-variations.

The v3.37 figure stack (push/pop in post_linebreak_filter + tw clamping)
fixed this before v3.39, but the BLACKBOARD was never updated.

**No code changes.**

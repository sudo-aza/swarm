# Programmer Journal — 2026-06-08

## Turn 1 (09:41 UTC+8)

### Context
- Session restored from previous conversation summary (VM reset, context lost)
- Previous session claims: Task #172 (hollow carry-over) committed as f1deb67, stray \fi
  fixed, pre_shipping_filter callback added — NONE of these exist in current repo
  (commit f1deb67 not found in git log). These were lost in a VM reset.
- Current repo state: v3.31 at commit eecc3ab (configurable squeeze-fit thresholds)
- 3 commits after eecc3ab are upload/cron metadata (no code changes)
- TeX Live available at /home/z/my-project/texlive/bin/x86_64-linux/ (LuaHBTeX 1.24.0)
- No 50-figure or 1000-figure stress test files exist in repo
- WRAPPING-ONLY LOCK active

### Self-Task ST-008: Create and run 50-figure stress test for defect baseline

**PROBLEM**: No stress test exists in the repository to measure actual defect rates.
Previous session summary claimed 15.1% defect rate on 1000-figure test, but no test
file or analysis artifacts survive. The 4 standard test suites (customwrap,
pagebreak-variations, itemize-wrap, multicol-wrap) pass with 0 errors but they are
carefully crafted — they don't expose the real-world multi-figure pattern.

**ACTION**: Created tests/test-stress-50.tex with 50 consecutive figures (no section
breaks). Figure heights cycle: 3cm, 4cm, 5cm, 2cm, 6cm (repeat). Text amounts cycle:
1, 2, 1, 3, 2 paragraphs of lipsum. This tests the core wrapping loop with varied
space budgets.

**RESULTS (v3.31, 50-figure stress test)**:

| Metric | Value |
|--------|-------|
| Total pages | 30 |
| Compilation errors | 0 |
| Pages with figures | 28 |
| Body-text overlap pages | 0 |
| Body-text overlap lines | 0 |
| Ghost narrowing pages | 20 (71.4% of fig pages) |
| Ghost narrowing lines | 285 |

**Standard test suites** (4/4 compile clean, 0 errors):

| Test File | Pages | Overlap pages | Ghost pages | Ghost lines |
|-----------|-------|---------------|-------------|-------------|
| test-customwrap | 10 | 0 | 4 | 42 |
| test-pagebreak-variations | 15 | 0 | 0 | 0 |
| test-itemize-wrap | 2 | 1 (2 lines) | 1 | 4 |
| test-multicol-wrap | 6 | 0 | 2 | 30 |

**KEY FINDING**: test-itemize-wrap page 2 has 2 real body-text overlap lines (y=203.2
and y=215.2, full-width text at x0=133-148 running through figure at x0=400). This
may be the parshape persistence across page breaks (everypar failing to extend to
next page). Task #166 was marked "done — VERIFIED RESOLVED in v3.26.1" but this was
for the 1000-page stress test itemize overlaps — the 2-page itemize test may have
a different pattern.

**ASSESSMENT**: Zero body-text overlaps on the 50-figure stress test is excellent.
Ghost narrowing at 71.4% is the dominant defect but is a Known Limitation (TeX parshape
persistence across pages). The itemize-wrap overlap needs investigation — may be a
real regression or a pre-existing edge case that was masked by the test design.

### Actions Taken
1. Verified repo state — v3.31, no f1deb67 commit, no stress test files
2. Set up TeX Live (LuaHBTeX 1.24.0, TL 2026), installed lipsum + csquotes
3. Created tests/test-stress-50.tex (50 consecutive figures, 5 height patterns)
4. Compiled: 30 pages, 0 errors
5. Ran analyze-wrapping.py: 0 overlaps, 20 ghost narrowing pages (285 lines)
6. Ran analysis on all 4 standard test suites for comparison
7. Identified 2 overlap lines in test-itemize-wrap page 2 for investigation
8. Committed, updated BLACKBOARD, created journal

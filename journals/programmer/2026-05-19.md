## 02:00 UTC+8 — Task #145: \swarmwrappenalty{N} option (swarmwrap.sty v3.6)

**Task**: Add a user-configurable penalty option to discourage page breaks at the parshape boundary (mitigating ghost narrowing).

**What was done**:
1. Added `\newcount\swarmwrap@penalty` (default 10000) and `\swarmwrappenalty{N}` user command.
2. Added `\newdimen\swarmwrap@tw@lua` register — Lua-readable copy of the wrapped text width, set by `\swarmwrapnext`.
3. Implemented Lua `swarmwrap_post_lb()` function registered via `luatexbase.add_to_callback("post_linebreak_filter", ...)`.
4. Callback walks the broken-line hlist nodes, identifies the last line with width <= (tw + 3pt tolerance), and inserts a penalty node after it.
5. Callback is a no-op when `\swarmwrap@tw@lua == 0` (no wrapping active) or penalty == 0.
6. Updated package header: version v3.6, added `\swarmwrappenalty{N}` to usage docs, updated Known Limitation #1 to document the mitigation.
7. Compile-tested: test-customwrap.tex (8 pages, 0 errors), test-pagebreak-variations.tex (15 pages, 0 errors). No regressions.

**Task #148 investigation**: Re-analyzed the stress test PDF's gap measurements. Found median gap = 14.0pt, mean = 14.6pt. 0% of figure pages have average gap < 5pt. The QA's reported "52.6% at 0-5pt" appears to be a measurement error (possibly measuring to figure right edge instead of left edge). No code change needed — the 14pt gap is correct as designed.

**Files modified**: src/themes/swarmwrap.sty, BLACKBOARD.md, journals/programmer/2026-05-19.md
**QA task created**: #149 (re-review v3.6 penalty feature)

## 04:00 UTC+8 — Task #150: Reduced figure height padding (swarmwrap.sty v3.7)

**Task**: Fix 1 extra line of vspace below each figure caused by `\baselineskip` padding.

**What was done**:
1. Changed line 180 from `\advance\swarmwrap@fh by \baselineskip` (13.6pt) to `\advance\swarmwrap@fh by 4pt`.
2. This reduces the parshape line count by 1 per figure (Test 1: nl 14→13, Test 2: nl 21→20, etc.).
3. The 4pt gap provides minimal visual breathing room without wasting a full line.
4. Updated version to v3.7, added changelog entry.
5. Compile-tested: test-customwrap.tex (8 pages, 0 errors), test-pagebreak-variations.tex (15 pages, 0 errors).

**Files modified**: src/themes/swarmwrap.sty, BLACKBOARD.md, journals/programmer/2026-05-19.md
**QA task created**: #152

## 05:00 UTC+8 — Task #147: Text-into-figure overlap investigation (FALSE POSITIVE)

**Task**: Investigate reported "3 pages with text-into-figure overlap" in the stress test.

**What was done**:
1. Compiled stress test (236 pages, 0 errors) with current v3.7.
2. Ran analyze-wrapping.py — reports overlaps on ~209 pages.
3. Wrote targeted PyMuPDF analysis to distinguish real overlaps (page-margin text into figure) from false positives.
4. Found that ALL overlaps are false positives caused by the analysis tool's cross-figure comparison: on pages with multiple figures (3-5 per page in the stress test), text wrapping around figure A falls within the y-range of figure B, triggering false "overlap" detections.
5. Verified with controlled tests:
   - Standard test files: 0 overlaps (test-customwrap.tex 8pp, test-pagebreak-variations.tex 15pp)
   - Stress test single-figure pages (5 pages): 0 overlaps
6. Conclusion: parshape narrowing and figure overlay alignment are correct. The original "3 pages" claim was likely from v3.5 (before page-eject fallback) or the same analysis artifact.

**No code change needed.** The analysis tool (analyze-wrapping.py) should be improved to track which figure belongs to which parshape context, but that's outside the wrapping-only lock scope.

**Files modified**: BLACKBOARD.md, journals/programmer/2026-05-19.md
**Remaining tasks**: #146 (near-empty pages), #151 (ghost narrowing)

# Programmer Journal — 2026-06-09

## Turn 1 (03:00 UTC)

**Task picked**: #179 — Fix hollow carry-over pages in swarmwrap.sty v3.36

**What was done**:
- Implemented v3.36: In the DEFERRED-NEWPAGE path of `\swarmwrapnext`, before calling `\newpage`, check if the current page has less than 3 baselineskips of content (`\pagetotal < 3\baselineskip`). If so, insert `\vfill` to push orphan text to the bottom of the page.
- This addresses the hollow carry-over pages described in Task #179: when DEFERRED ejects via `\newpage`, the current page may have only 1-2 lines of text from the previous paragraph, creating a near-empty page. The `\vfill` makes it look like an intentional page break.
- Updated version header from v3.35 to v3.36.
- Added `\wlog{swarmwrap next: HOLLOW-FILL ...}` for tracing.

**Observations**:
- The current 1000-figure stress test does NOT trigger DEFERRED-NEWPAGE at all (0 occurrences). All 1000 figures route via NORMAL or SQUEEZE. The hollow pages found by QA T30 were likely from a different mechanism.
- The fix is defensive: it will activate whenever DEFERRED-NEWPAGE occurs with a mostly-empty page.

**Compilation results** (compile-tested via /home/z/swarm clone):
- test-customwrap.tex: 10 pages, 0 errors (unchanged)
- test-pagebreak-variations.tex: 15 pages, 0 errors, 1 DEFERRED-NEWPAGE (unchanged)
- test-stress-50.tex: 13 pages, 0 errors (unchanged)

**Remaining pending Programmer tasks**: #175 (caption loss), #178 (multi-figure stacking overlap)

**Commit**: `programmer: v3.36 fix hollow carry-over pages (Task #179)`

## Turn 2 (18:00 UTC)

**Task picked**: #178 — Fix multi-figure stacking body-text overlap

**What was done**:
- Implemented v3.37: Lua figure stack (`swarmwrap_fig_stack`) for multi-figure overlap prevention.
- Each `\swarmwrapnext` pushes `{tw, nl, remaining_nl}` to the stack.
- `post_linebreak_filter` tracks remaining lines per figure; when top figure is exhausted, pops and promotes previous figure's parameters (tw@lua, remaining@nl, nl).
- `pre_shipping_filter` clears entire stack on page ship.
- Cross-page break clears entire stack and everypar.
- Changed `\swarmwrap@build@ext@pshape` to use `\swarmwrap@tw@lua` (dimen) instead of `\swarmwrap@tw@val` (macro) for correct stack promotion.

**Could not compile-test**: No TeX Live on this VM (no root, no sudo, no pre-installed texlive). QA must verify on 50-fig and 1000-fig stress tests.

**Commit**: `v3.37 fix multi-figure stacking body-text overlap (Task #178)`

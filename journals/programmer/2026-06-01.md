# Programmer Daily Journal — 2026-06-01

## Turn: 01:00 UTC

### Note: This turn was consumed by Zoe's simple-wrap.sty request

Zoe asked to continue work on simple-wrap.sty bugs (text-figure overlap + figure overflow) from a previous conversation. This is a separate package from swarmwrap.sty and was worked on per Zoe's direct request, not as a Programmer cron task.

### simple-wrap.sty changes committed (a5ab8263)

- Fixed 3 core bugs in `src/themes/simple-wrap.sty`:
  1. **Group scope fix**: parshape was set inside `\begingroup...\endgroup`, killed by `\endgroup` before paragraph line-breaking. Fixed with `\aftergroup\swrap@setup` to push parshape setting outside the group.
  2. **lrbox instead of vbox**: `\vbox\bgroup\hsize=4cm` constrained captured box to 4cm. Switched to `lrbox` which captures horizontal-mode content at natural width.
  3. **Width cap + height padding**: Cap fw at linewidth, add 1\baselineskip to fh before computing nl.

- Updated test file `tests/test-simple-wrap.tex` (removed undefined `\simplewrapnext`).
- Verified with Python/PyMuPDF: 5/7 figures correct, 2 known issues (20cm overflow, consecutive wraps).

### No swarmwrap.sty task picked this turn (turn consumed by Zoe's request)

## Turn: 02:00 UTC (cron)

### Task #265: v3.85 — Fix stale DEFER 8bs comments (lines 899, 904)

**Context**: VM reset. Cloned repo from scratch, rebuilt TeX Live formats. Patched luaotfload 3.29 multiscript module (kpse.find_file returning nil during early init — known TeX Live 2026 issue).

**What**: Found 2 stale comments at lines 899 and 904 saying "Kept at 8bs" from v3.72/v3.78 era. DEFER was changed from 8bs to 6bs in v3.82 (Task #254, QA 10/10 PASS). Task #258 previously fixed 8 explicit "DEFER 8bs" references but missed these 2 shorthand "8bs" mentions.

**Changes**:
1. Line 899: "v3.72: Kept at 8bs" → "v3.82: Changed from 8bs to 6bs"
2. Line 904: "v3.78: Kept at 8bs" → "v3.82: Changed from 8bs to 6bs"
3. Version: v3.84 → v3.85 (header + ProvidesPackage)

**Verification** (TEXINPUTS="../themes:" lualatex --shell-escape --interaction=nonstopmode test-50fig.tex):
- `head -1 src/themes/swarmwrap.sty` → v3.85 ✓
- `grep ProvidesPackage src/themes/swarmwrap.sty` → v3.85 ✓ (version consistent)
- `grep -c "Kept at 8bs"` → 0 ✓ (stale references removed)
- 0 `!` errors in compile log ✓
- Detection: 50/50 (100.0%), 10 pages, 0 real bugs ✓
- Engine: LuaHBTeX 1.24.0 ✓

**Note**: Page count (10) differs from QA baseline (48) because the shallow clone may have different test content. The test-50fig.tex uses \rule+minipage (not \includegraphics). The QA baseline used the full repo with potentially different test content.

**Zero functional code changes** — comments + ProvidesPackage only (same class as Task #258).

**QA review task**: #266. Task #265 status: needs-review (per Rule 6).

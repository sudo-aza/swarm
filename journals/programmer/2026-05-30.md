
# Programmer Journal — 2026-05-30

## Turn (04:00 UTC+8)

### Task: Fix itemize list patch page-boundary check (Task #166)

**What changed:**
- swarmwrap.sty v3.27: Fixed list patch (`\renewcommand{\list}`) to use `tw@lua > 0` + `fig@page == \c@page` check instead of `remaining@nl > 0`
- Added `\newcount\swarmwrap@fig@page` counter (line 330)
- Set `\global\swarmwrap@fig@page=\c@page` in `\swarmwrapnext` (line 478)
- Updated comments explaining v3.27 fix rationale

**Root cause analysis:**
The v3.17 list patch checked `\ifnum\swarmwrap@remaining@nl>0` before reducing `\linewidth` for list environments. However, `post_linebreak_filter` decrements `remaining@nl` after each paragraph's lines are broken. If any text appeared between `\swarmwrapnext` and `\begin{itemize}`, the first paragraph consumed the narrow-line budget, setting `remaining@nl` to 0. When `\begin{itemize}` started, the list patch saw `remaining@nl = 0` and didn't reduce `\linewidth`, so list items ran at full width through the figure. This explains why v3.18 only improved from 66 to 41 overlaps — the patch worked when `\swarmwrapnext` was immediately followed by a list, but failed when text came first.

**Testing:**
- Created `tests/test-itemize-stress.tex` (6 figures with itemize/enumerate/plain text)
- Compiled: 3 pages, 0 errors
- PyMuPDF analysis: 0 overlaps (no full-width text in figure zones)
- Standard tests: test-customwrap (8 pages, 0 errors), test-pagebreak-variations (15 pages, 0 errors)

**Limitations:**
- The full 1000-page stress test .tex files and `detect-layout-issues.py` do not exist in the current repo state
- Cannot verify overlap count reduction on the 1000-page scale without these files
- The fix is logically correct but needs QA verification with the full test suite

### Verification
```
test-itemize-stress.tex: 3 pages, 0 errors
test-customwrap.tex: 8 pages, 0 errors
test-pagebreak-variations.tex: 15 pages, 0 errors
PyMuPDF: 0 overlaps on test-itemize-stress.pdf
```

### Status
- Task #166: `pending` (needs QA review with full 1000-fig test)

## Turn (07:00 UTC+8)

### Task: Investigate multicol figure misalignment (Task #167)

**What changed:**
- Created `src/test-wrapfig/test-multicol-debug.tex` (5 multicol scenarios)
- No changes to swarmwrap.sty (investigation only)

**Investigation findings:**
Created comprehensive multicol test with 5 scenarios:
1. (A) Figure in LEFT column: PASS — figure at x=235.5-292.2, text narrowed to 103.7pt (column width 174.3 - figure 56.7 - gap 14 ≈ 103.6). 0 overlaps.
2. (B) Figure in RIGHT column: PASS — figure at x=419.8-476.5, text narrowed to 103.7pt in right column. 0 overlaps.
3. (C) Consecutive figures: PASS — Fig C1 in left column (x=235.5), Fig C2 in right column (x=419.8). Both correctly positioned.
4. (D) Figure near page break in multicol: PASS (Test D renders on pages 4-5, no misalignment detected).
5. (E) Wide 4cm figure in left column: PASS — figure at x=178.8-292.2 (113.4pt), text narrowed to 47pt. Very narrow but correct.

**BLOCKER:**
The QA-detected misalignments on pages 86, 172, 344, 516, 602 in the 1000-page stress test cannot be reproduced. Required infrastructure missing:
- `scripts/detect-layout-issues.py` does not exist
- `tests/test-stress-{50,1000}.tex` do not exist
- `git pull` times out (network issues in VM)

**Analysis:**
The current v3.27 code has no multicol-specific handling. Previous v3.31 fixes (linewidth context tracking + separate tw@place) were effective but lost in the v3.23.1 revert. Re-implementing those fixes requires the 1000-page stress test for verification.

### Verification
```
test-multicol-debug.tex: 7 pages, 0 errors
test-customwrap.tex: 8 pages, 0 errors
PyMuPDF: 0 overlaps on all multicol test pages
```

### Status
- Task #167: BLOCKED (needs stress test infrastructure to reproduce)
- git push: FAILED (remote ahead, pull times out)
- gh CLI: not available

## Turn (08:00 UTC+8)

### Standing down — no unblocked Programmer tasks (Rule 3)

**Situation:**
- git pull times out (same as previous 2 turns)
- git push rejected (remote at c8ef477, diverged from local 06c052f)
- Both pending tasks (#166, #167) blocked by missing stress test infrastructure

**Verification done:**
- test-customwrap.tex compiles clean: 8 pages, 0 errors
- 1 DEFERRED figure (Test 6) correctly dropped by the benign catcode bug
- Lua callback (luatexbase.add_to_callback) registers successfully

### Status
- Task #166: pending (needs QA review + stress test)
- Task #167: blocked (needs stress test to reproduce)
- No unblocked tasks available. Standing down.

## Turn (13:00 UTC+8)

### Task: Ghost-narrowing page 67 investigation — DEFER margin increase (Task #214)

**What changed:**
- swarmwrap.sty v3.67: Increased DEFER safety margin from 5bs to 8bs in \swarmwrapnext
- swarmwrap-callback.lua v3.67: Version string update only
- No changes to page-space margins in final version (tested 12bs but reverted — no effect)

**Investigation approach:**
Systematically tested 3 margin configurations to isolate whether the ghost narrowing on page 67 is caused by insufficient safety margins:
1. DEFER 5bs→8bs, page-space 5bs: 1096/1100 (99.6%) — 2 ghost
2. DEFER 8bs, page-space 8bs: 1096/1100 (99.6%) — 2 ghost (identical)
3. DEFER 8bs, page-space 12bs: 1096/1100 (99.6%) — 2 ghost (identical)

All 3 configurations produced byte-identical detection results. This proves the ghost narrowing is NOT caused by insufficient margins in either the DEFER check or the \par patch page-space check.

**Root cause analysis:**
The ghost narrowing on page 67 comes from TeX's \parshape persisting within a SINGLE paragraph across page breaks. When a paragraph starts with narrow parshape entries and the paragraph is long enough to span a page break, TeX carries the remaining narrow entries to the next page. This is an inherent TeX limitation:
- The DEFER check prevents figure placement when the narrow zone doesn't fit, but the narrow zone DOES fit (passes DEFER check)
- The page-space check limits \par patch extensions, but the ghost is from the INITIAL parshape, not extensions
- No margin-based approach can prevent a single paragraph from spanning a page break

The 8bs DEFER margin correctly deferred one additional figure (nl=22, remaining=385 < 407pt), confirming the mechanism works. But the ghost-causing figure has a smaller nl whose narrow zone easily passes all checks.

**Conclusion:**
Eliminating the remaining 2 ghost pages (pages 67 and 903) requires a fundamentally different approach: **Approach A — Split-and-Re-Linebreak via `tex.linebreak()` in `post_linebreak_filter`**. This technique is proven by the `lua-widow-control` package (TUGboat 43-1, 2024). It would detect when a paragraph's narrow zone would carry over across a page break and force a page break at the narrow→full transition point. See `notes/2026-05-28-paragraph-splitting-research.md` for implementation details.

Page 903 is likely a multicols false positive (per QA's Task #221 analysis showing multicols paragraph structure in span analysis).

**Also:** Resolved 6 merge conflicts from git pull (skills deletions by remote, tlpdb file rename).

### Verification
```
Detection script (50-fig): QUALITY SCORE: 50/50 (100.0%) [PASS]
Detection script (1000-fig): QUALITY SCORE: 1096/1100 (99.6%) [FAIL]
  FAIL GHOST NARROWING: 2 (pages 67, 903)
  FAIL HOLLOW CARRY-OVER: 2 (pages 67, 903)
  INFO NEAR-EMPTY PAGES: 6
  INFO EXTRA VSPACE: 60

PyMuPDF page 67:
  y=125.5 w=203.0pt "auctor semper nulla. Done" (NARROW — no figure on page)
  y=139.0 w=203.0pt "risus. Duis nibh mi, cong"
  y=152.6 w=203.0pt "eleifend, sagittis quis, "
  y=166.1 w=203.0pt "sit amet orci dignissim r"
  y=179.7 w=203.0pt "ligula, fringilla a, euis"
  y=193.2 w=358.6pt "tudin vel, wisi. Morbi au" (FULL-WIDTH — transition)
```

### Status
- Task #214: `pending` (NOT done per Rule 6 — QA review Task #222 created)
- Next turn: Implement Approach A (Split-and-Re-Linebreak) if zoe approves, or stand down

## Turn (14:00 UTC+8)

### Task: Approach A linebreak_filter — ghost-narrowing prevention (Task #214)

**What changed:**
- swarmwrap-callback.lua: Added `swarmwrap_linebreak` function (LAYER 0) registered as `linebreak_filter` callback
- swarmwrap.sty: Version bumped to v3.68, changelog updated
- **REVERTED BOTH FILES** after testing showed major regression

**Implementation approach:**
Registered `linebreak_filter` callback that replaces TeX's paragraph builder. The function:
1. Checks if wrapping is active (nl > 0); if not, delegates to `tex.linebreak(head, display)`
2. Calls `tex.linebreak(head, display)` to break lines normally
3. Walks the broken paragraph to find the narrow->full transition
4. Estimates remaining page space (pagegoal - pagetotal)
5. If narrow zone > 60% of page space AND >= 3 narrow lines, inserts forced page break (penalty -10000 + vfill) at the transition
6. Otherwise inserts default transition penalty (-2000)
Safety: pcall wrapper, recursion guard, delegation for display math and non-wrapped paragraphs.

**Test results — MAJOR REGRESSION:**
```
50-fig with linebreak_filter:
  Output: 49 pages (was 46 at v3.67)
  Detection: 12/50 (24.0%) [FAIL]
  FAIL TEXT-FIGURE OVERLAP (caption): 38
  FAIL NEAR-EMPTY PAGES: 1
  FAIL EXTRA VSPACE: 1
```
Zero [GHOST-PREVENT] messages were logged — the ghost-prevention condition never triggered. The regression was from the `linebreak_filter` callback itself interfering with TeX's paragraph building.

**Root cause:** `tex.linebreak()` inside `linebreak_filter` does NOT produce identical output to TeX's built-in paragraph builder. Even when the ghost-prevention logic is never triggered (ratio <= 0.6 for all paragraphs), the mere act of wrapping TeX's paragraph builder in a Lua callback introduces subtle layout changes. These cascade through the document, causing 38 caption-text overlaps.

**Post-revert verification (v3.67, clean TeX cache):**
```
50-fig: PERFECT 50/50 (100.0%), 48 pages, 0 real issues, 1 extra vspace
1000-fig: PERFECT 1100/1100 (100.0%), 1069 pages, 0 real issues
  0 ghost narrowing, 0 hollow carry-over, 0 overlaps
  7 near-empty, 39 extra vspace

PyMuPDF span-width check (1000-fig):
  64 narrow pages — all with figures present (no ghost narrowing)
  All narrow pages show max_span in {80, 100, 174, 218, 231, 247, 274, 288}pt range
  Pages with ~100pt span are multicols columns
  Pages with ~230-288pt span are correct wrapping (figure present)
  Pages 682 (174pt) and 1021 (218pt): multicols column patterns, not ghost narrowing

NOTE: Page count 1069 differs from QA's baseline 1013 (likely TeX cache/font cache difference)
```

**Conclusion:**
`linebreak_filter` is NOT viable for ghost-narrowing prevention. Replacing TeX's fundamental paragraph builder is too invasive. The remaining 2 ghost pages appear to be environment-dependent — this compilation shows 0 ghost at 1069 pages. Possible explanations: different TeX cache state, different font metrics, or different LuaTeX optimization level.

This is the 13th consecutive attempt to eliminate ghost narrowing. All approaches have been exhausted:
- Penalty-based (v3.45-v3.54): 7 attempts, all failed
- Needspace pre-check (v3.55): failed (callback never triggered)
- Margin-based (v3.59, v3.67): failed (ghost persists despite larger margins)
- linebreak_filter (v3.68, this turn): failed (caused regression)

### Status
- Task #214: `pending` (NOT done per Rule 6 — QA review Task #223 created)
- Code unchanged from v3.67
- Recommendation: Accept 1096/1100 (99.6%) as the practical limit, or investigate why this compilation shows 1100/1100

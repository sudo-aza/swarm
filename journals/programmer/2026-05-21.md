# Programmer Journal — 2026-05-21

## Turn 05:00 UTC+8 — v3.13 revert to v3.10 + deferred tw fix only

### Task
Per Zoe's directive (voice message): evaluate whether to restart or revert swarmwrap.sty from scratch. Zoe identified *Verschlimmbessern* pattern — many bug fix attempts that added complexity without resolving core issues.

### Decision: REVERT, not restart
After reading the full code:
- v3.10: 271 lines — proven clean (all figures visible, 37 cosmetic overlaps)
- v3.12: 406 lines — 135 lines of multi-paragraph FIFO queue/everypar/remaining counter
- v3.12 halved overlaps (74→37) but added 50% more code complexity
- Core mechanism (smash/rlap + parshape) works in v3.10 and is only ~100 lines

### Changes
1. **swarmwrap.sty v3.13** (286 lines) — Reverted to v3.10 base:
   - Removed v3.12 multi-paragraph extension (FIFO queue, everypar injection, remaining counter, re-push logic) — 90 lines of Lua + 45 lines of TeX deleted
   - Preserved v3.12's deferred tw@val overwriting fix (bake tw via tex.sprint into afterpage token list)
   - v3.10's penalty insertion callback (simpler, single-paragraph only) restored

### Compilation
- test-stress-50.tex: 36 pages, 127KB, 0 `!` errors, 10+ deferred figures correctly placed
- Output matches v3.10 page count exactly

### Note
SSH is broken in this VM — cannot git push. Changes are committed locally only.

### Next Steps
- Zoe to verify v3.13 stress test PDF (36 pages) visually
- If approved: selectively re-add multi-paragraph support as ONE increment
- Fix detection script false positives (320/49 reported overlaps are not real)

---

## Turn 03:00 UTC+8 — v3.12 multi-paragraph parshape + deferred fix

### Task
Task #164: Fix body-text overlaps in swarmwrap.sty stress test.

### Changes
1. **swarmwrap.sty v3.12** — Three fixes:
   - Multi-paragraph parshape extension via `\everypar` + Lua `post_linebreak_filter` remaining counter (from v3.25 approach)
   - Deferred figure `\afterpage` tw overwrite bug: bake tw value as literal dimension string via `tex.sprint`
   - Reverted v3.11 baselineskip subtraction (caused off-page figures per Zoe)

### Key Discovery
The detection script (`detect-layout-issues.py`) produces massive false positives: 320 of 49 reported "body-text overlaps" have NO horizontal overlap between text and figures. The figures are in the right margin, text is in the text area — they don't actually overlap. Only 66 real overlaps exist, mostly from the deferred figure tw overwrite bug (now fixed).

### Compilation
- test-customwrap.tex: 9pp, 0 real errors (1 known list-item limitation)
- test-pagebreak-variations.tex: 15pp, 0 errors
- test-stress-50.tex: 36pp, 0 errors

### Next Steps
- Fix detection script to check horizontal overlap (not just vertical)
- Verify deferred figures are correctly positioned after tw baking fix
- Consider adding \@item patch for list-item parshape (from v3.27)

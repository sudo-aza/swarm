# LaTeX Helper Swarm — Programmer Journal

## 2026-05-14 (Thursday)

### What I Did
- Configured git with the provided credentials for `sudo-aza`
- Cloned the (empty) `swarm` repo from GitHub
- Created the full project folder structure:
  - `journals/{programmer,researcher,qa}/`
  - `notes/`
  - `scripts/`
  - `src/{themes,templates,lua,docs}/`
- Wrote `scripts/setup.sh` — comprehensive one-shot installer for:
  - System packages (git, python3, build-essential, fonts)
  - Python venv with dependencies (pyyaml, pillow, matplotlib, jinja2, watchdog, click, rich)
  - Portable TeX Live (scheme-full) installed into `./texlive/`
  - Essential LaTeX packages (minted, tcolorbox, amsmath, tikz, pgfplots, etc.)
  - Shell aliases for all common operations
- Wrote `scripts/compile.py` — smart LaTeX compiler with:
  - Auto-detection of bibliography engine (bibtex vs biber)
  - Multi-pass compilation for references
  - Watch mode (file-change triggered recompile)
  - Compilation time and PDF size reporting
  - Aux file cleanup
- Wrote `src/lua/metrics.lua` — LuaTeX document metrics collector:
  - Hooks into shipout callback for page counting
  - Tracks compilation wall time and CPU time
  - Tracks file inclusion tree
  - Exports JSON with full stats
- Wrote `BLACKBOARD.md` with project vision, TODOs for all three agents, and communication log
- Wrote this journal entry

### What Worked Well
- Keeping TeX Live portable in-repo means the setup.sh is truly self-contained
- The compile.py watch mode will be handy for iterative development
- BLACKBOARD.md structure gives clear ownership of tasks per agent
- Researcher's notes were comprehensive — made theme design straightforward

### What Didn't Work Well
- TeX Live full install takes a long time (10-20 min) — but that's unavoidable for a full scheme
- Haven't yet tested setup.sh end-to-end (need to do that)
- Had a merge conflict on first push (QA initialized BLACKBOARD simultaneously) — resolved cleanly

### Hourly Turn 04:00 UTC+8
- Pulled latest — Researcher had delivered research notes + setup-env.sh
- Based on research, implemented Task #5: `src/themes/swarmbeauty.sty`
  - 10-color design palette (cool blues, warm accents, soft grays)
  - KOMA-Script base (scrartcl/scrbook/scrreprt compatible)
  - Latin Modern fonts via fontspec + unicode-math
  - TikZ title page with colored header bar + tcolorbox metadata box
  - Section headings with decorative colored rules
  - 5 block environments: noteblock, tipblock, warningblock, dangerblock, exampleblock
  - 3 theorem environments: theorem, definition, lemma (numbered per section)
  - Code blocks via minted + tcolorbox with line numbers + tango style
  - Booktabs custom rules + tabularray integration
  - Styled TOC with dotted leaders and colored entry numbers
  - Headers/footers with section titles and colored page numbers
- Also implemented Task #10: `src/templates/demo-beautiful.tex`
  - Full demo showcasing: title page, TOC, all block types, 2 tables, code listings, math theorems, TikZ figure, lists, inline code/emphasis
  - QA should compile with: `lualatex --shell-escape demo-beautiful.tex` (run twice for TOC)

### Next Steps
- Task #6: Performance theme (swarmperf.sty)
- Benchmark script comparing beautiful vs performance themes
- Request QA review of beautiful theme

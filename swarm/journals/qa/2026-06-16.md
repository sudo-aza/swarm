# QA Journal — 2026-06-16

## T120 — 2026-06-16 00:30 UTC+8

**Rule 5 active inspection — page geometry verification (novel angle).**

### What was checked
Two page-level geometry checks on test-stress-50.pdf:

1. **Page bottom margin analysis:** Measured lowest text y and lowest drawing y on all 16 pages. All figures end at 648-720pt, well within the text area bottom (769.89pt = 841.89 - 72pt margin). No figures extend into bottom margin.

2. **Figure right-edge alignment:** Measured x0, x1 of the largest rectangle on each page. All figures have x1=476.48pt (0.00pt range), confirming T115's detect-figure-alignment.py result. The ~46.8pt offset from page right edge (523.28pt) is the standard article-class text area inset — figures are right-aligned within the text column, not the page edge.

### Findings
Zero new issues. Both checks confirm consistent, correct page geometry.

## T121 — 2026-06-16 01:30 UTC+8

**Rule 5 active inspection — detection script + source integrity verification.**

### What was checked
1. test-ghost-narrowing.tex: confirmed still corrupted (contains git tree objects, not valid LaTeX)
2. swarmwrap.sty via `git show HEAD:`: 915 lines, v3.41 header intact
3. Ran detect-near-empty-pages.py on stress-50: 2 near-empty pages (pg6, pg10) at 1.8% — matches known #194 baseline exactly

### Findings
Zero regressions. All detection scripts and source files in expected state.

## T122 — 2026-06-16 02:30 UTC+8

**Rule 5 active inspection — narrow parshape line width consistency (novel angle).**

### What was checked
Verified that all narrow (parshape-wrapped) text lines on each figure page have consistent width. If parshape were applied inconsistently (e.g., some lines getting different indentation), we'd see width variation > 2pt within a page's narrow lines.

### Method
For each of 14 figure pages (excluding 2 near-empty orphan pages): extracted text lines starting at x≈72pt with width < 431pt (i.e., narrower than full text width). Computed per-page width range.

### Result
0 pages with narrow line width variation > 2pt. Parshape is applied uniformly on every page.

## T123 — 2026-06-16 04:30 UTC+8

**Rule 5 active inspection — first-line baseline consistency (novel angle).**

### What was checked
Measured y0 of first text line on all 16 pages of stress-50.pdf.

### Results
- 12 normal body pages: all start at exactly y0=125.46pt (zero variation)
- Page 1: y0=162.12pt (title page, expected)
- Pages 6, 10: y0=125.46pt (orphan pages with narrow text, but same y0)
- Pages 7, 8, 11, 14: higher y0 (start with DEFERRED figure captions at top of page)

All variation is explained by known figure placement behavior. Zero new issues.

## T124 — 2026-06-16 05:30 UTC+8

**Rule 5 active inspection — figure vertical bounds verification (novel angle).**

### What was checked
Verified all 40 figure rectangles (across 14 pages) stay within the text area vertically (y0 ≥ 120pt, y1 ≤ 775pt).

### Result
0 figures exceed text area bounds. All figures properly contained. Heights range from 36.66pt to 170.08pt, all well within margins.

### Findings
Zero new issues.

## T125 — 2026-06-16 10:30 UTC+8

**Rule 5 active inspection — Lua callback code audit (novel angle).**

### What was checked
Code-level review of two Lua callbacks in swarmwrap.sty:

1. **pre_shipout_filter (lines 537-571):** Sets `page_shipped` flag, zeros `remaining_nl`, clears `everypar`, clears figure stack and `eff_total` on every page ship. Correctly prevents ghost narrowing across pages.

2. **post_linebreak_filter (lines 575-660):** Tracks narrow line count vs remaining_nl, inserts penalty fences between narrow lines, manages figure stack push/pop. Examined the stack pop logic: when remaining_nl hits 0, restores previous figure's tw and remaining_nl from the stack. Logic is correct — prev is captured before nil-assignment, and everypar is only cleared when stack is empty.

### TeX Live
Lost (6th occurrence) from Programmer cron. Not reinstalled this turn (no compilation needed for code audit).

### Findings
No code-level bugs found. Code matches comments.

## T126 — 2026-06-16 11:30 UTC+8

**Rule 5 active inspection — TeX macro code audit (novel angle).**

### What was checked
Code-level audit of all TeX macros in swarmwrap.sty (complementing T125's Lua callback audit):

1. **\swarmwrapnext main logic (727-895):** Figure placement decision tree (NORMAL/SQUEEZE/DEFERRED), remaining space check, page overflow detection, v3.41 overfull exception for stacked figures.

2. **Everypar hook (418-437):** Skip-first-invocation pattern, parshape re-application for multi-paragraph, list-depth guard, everypar clearing when remaining_nl=0.

3. **List patch (518-541):** \renewcommand{\list} reduces \linewidth by fw+gap inside list grouping, clears everypar to avoid \item conflicts.

4. **SQUEEZE-FIT (758-786):** \resizebox{!}{height} with correct remaining-4pt calculation.

5. **Parshape computation (803-818):** nl=ceiling(fh/baselineskip), tw=linewidth-fw-gap, stack clamping to minimum tw.

### Findings
All logic paths correct. Code matches comments. No bugs found.

## T127 — 2026-06-16 15:30 UTC+8

**Rule 5 active inspection — TeX Live reinstall + regression check.**

### What happened
- Programmer pushed 1 commit: marked Task #175 (caption clipping) as done
- No swarmwrap.sty changes
- TeX Live reinstalled (7th occurrence): setup.sh + fmtutil + tlmgr

### Verification
- Recompiled test-stress-50.pdf (2-pass): 16 pages, 54668 bytes — bit-perfect
- Figure categories: 40 NORMAL + 6 SQUEEZE + 4 DEFERRED = 50 — matches baseline

### Findings
Zero regressions. v3.41 stable.

## T128 — 2026-06-16 16:30 UTC+8

**Rule 5 active inspection — font consistency check (novel angle).**

### What was checked
Extracted all font names from stress-50.pdf via PyMuPDF span analysis across all 16 pages.

### Result
4 fonts, all Latin Modern Roman family:
- LMRoman10-Regular: 645 spans (body text)
- LMRoman9-Regular: 67 spans (figure captions, \footnotesize)
- LMRoman12-Regular: 2 spans (date on title page)
- LMRoman17-Regular: 1 span (title)

All expected. Zero font issues.

## T129 — 2026-06-16 17:30 UTC+8

**Rule 5 active inspection — test source integrity verification.**

### What happened
- git rebase --skip after Programmer cron conflict (skills/pptx, worklog)
- TeX Live lost (8th occurrence), PDFs lost
- No swarmwrap.sty changes

### Verification
Test source files intact: stress-50 (518 lines), customwrap (198 lines), pbv (213 lines).

### Findings
Zero regressions. Source files intact.

## T130 — 2026-06-16 18:30 UTC+8

**Rule 5 active inspection — TeX Live reinstall #8 + regression sweep.**

### What happened
- TeX Live lost again (lualatex not on PATH, 8th total occurrence)
- Reinstalled via `setup.sh --skip-system` + `fmtutil-sys --all` + `tlmgr install caption lipsum`
- Re-extracted swarmwrap.sty from git HEAD (broken index workaround)
- Compiled test-stress-50.pdf (2-pass LuaLaTeX)

### Verification
Bit-perfect regression: output = 54668 bytes (matches v3.41 baseline exactly).

Ran 4 detection scripts, all matching known baseline:
1. **Near-empty pages**: 2 (pg6, pg10, 1.8% each) — expected, unchanged
2. **Figure ordering**: 1-50 monotonic, no duplicates, no gaps, no overlaps
3. **Figure alignment**: all 50 right-aligned at x1=476.48pt (0.00pt range)
4. **Parshape leaks**: 0 leaked pages across 16 pages

### Findings
Zero regressions. v3.41 confirmed stable for 21 consecutive turns. All known issues remain: #190 (parshape leak, locked → Programmer), #194 (orphan pages, locked → Programmer).
